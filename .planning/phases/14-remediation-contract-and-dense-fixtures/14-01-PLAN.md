---
phase: 14
plan: 01
type: implementation
wave: 1
depends_on:
  - 13
files_modified:
  - src/ir/diagram.ts
  - src/ir/index.ts
  - src/solver/solve.ts
  - src/dsl/schema.ts
  - src/dsl/normalize.ts
  - src/dsl/render.ts
  - test/dense-acceptance.test.ts
  - test/public-api.test.ts
  - README.md
  - README.zh-CN.md
autonomous: true
requirements:
  - CONTRACT-01
  - CONTRACT-02
  - CONTRACT-03
  - CONTRACT-04
  - FIXTURE-01
  - FIXTURE-02
  - FIXTURE-03
  - FIXTURE-04
---

# Phase 14 Plan 01: Remediation Contract And Dense Fixtures

<objective>
Add the public remediation contract and dense fixture/evidence baseline that later phases execute against.
</objective>

<threat_model>
No external services. Compatibility risk is public API drift; mitigate with additive types, public API tests, and preserved `strict`/`deliverability` behavior.
</threat_model>

<tasks>
<task id="T14-01" type="execute">
  <read_first>
    - `src/ir/diagram.ts`
    - `src/solver/solve.ts`
    - `src/ir/index.ts`
    - `test/public-api.test.ts`
  </read_first>
  <action>Add public types for dense remediation: `DeliverabilityMode`, `RemediationPolicyMode`, `RemediationPolicy`, `RemediationPlan`, and type-specific detail records for external labels, route rails, growth, and page split suggestions. Add optional `remediationPlans` to `DeliverabilityReport` or `CoordinatedDiagram` while preserving existing `remediationTypes`.</action>
  <acceptance_criteria>
    - TypeScript exports include the new remediation contract types.
    - Existing fields `deliverability.status`, `deliverability.remediationTypes`, and `degraded` remain available.
    - `rtk npm run typecheck` exits 0.
  </acceptance_criteria>
</task>

<task id="T14-02" type="execute">
  <read_first>
    - `src/solver/solve.ts`
    - `src/ir/diagnostics.ts`
    - `.planning/phases/14-remediation-contract-and-dense-fixtures/14-RESEARCH.md`
  </read_first>
  <action>Add a deterministic remediation plan builder that maps current deliverability diagnostics to plan objects. It must produce plan types for external labels, route rails, grow fixed geometry, and page split suggestions using sorted edge/node IDs and stable plan IDs. This phase may leave plan status as `suggested`; execution status is for later phases.</action>
  <acceptance_criteria>
    - Degraded dense fixtures produce at least one remediation plan object.
    - Plan IDs and edge/node ID lists are stable across repeated solves.
    - `rtk npm test -- test/dense-acceptance.test.ts test/determinism.test.ts` exits 0.
  </acceptance_criteria>
</task>

<task id="T14-03" type="execute">
  <read_first>
    - `src/dsl/schema.ts`
    - `src/dsl/normalize.ts`
    - `src/dsl/render.ts`
    - `test/dsl.test.ts`
  </read_first>
  <action>If DSL metadata already forwards routing options, extend it to accept and normalize `deliverabilityMode` and `remediationPolicy`. If DSL forwarding would create broad churn, keep the contract API-only in this phase and document DSL support as deferred in `14-SUMMARY.md`.</action>
  <acceptance_criteria>
    - Either DSL tests prove metadata forwarding for the new options, or `14-SUMMARY.md` explicitly states API-only support for this phase.
    - `rtk npm test -- test/dsl.test.ts` exits 0 when DSL files are changed.
  </acceptance_criteria>
</task>

<task id="T14-04" type="execute">
  <read_first>
    - `test/dense-acceptance.test.ts`
    - `test/solver.test.ts`
    - `src/ir/diagram.ts`
  </read_first>
  <action>Extend dense acceptance fixtures to include CV dependency with at least 20 labeled edges, OV/SV resource-flow, and IBD/high-fan-in shapes. Extend `stage5Evidence` to count route/obstacle intersections and remediation plan counts/types.</action>
  <acceptance_criteria>
    - `test/dense-acceptance.test.ts` includes fixtures for CV, OV/SV, and IBD/high-fan-in.
    - Stage 5-style evidence includes `routeObstacleIntersections` and remediation plan information.
    - `rtk npm test -- test/dense-acceptance.test.ts` exits 0.
  </acceptance_criteria>
</task>

<task id="T14-05" type="execute">
  <read_first>
    - `README.md`
    - `README.zh-CN.md`
    - `src/ir/diagram.ts`
  </read_first>
  <action>Document the new public contract: dense deliverability mode, remediation policy modes, remediation plan objects, and compatibility with `strict`/`degraded`/`deliverability.remediationTypes`.</action>
  <acceptance_criteria>
    - `README.md` contains `deliverabilityMode` and `remediationPolicy`.
    - `README.zh-CN.md` contains equivalent dense deliverability documentation.
    - `rtk npm run lint` exits 0.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- `rtk npm test -- test/dense-acceptance.test.ts test/public-api.test.ts test/determinism.test.ts`
- `rtk npm run typecheck`
- `rtk npm run lint`
</verification>

<success_criteria>
- The remediation contract is public, typed, documented, and deterministic.
- Dense fixtures exist before remediation execution changes.
- Existing strict/degraded behavior remains backward compatible.
</success_criteria>
