---
phase: 04-coordinated-exporters
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - src/index.ts
  - test/public-api.test.ts
  - test/exporters.test.ts
  - test/determinism.test.ts
  - test/fixtures/phase-04/coordinated-export.canonical.json
  - test/fixtures/phase-04/coordinated-export.svg
  - test/fixtures/phase-04/coordinated-export.excalidraw.json
  - biome.json
autonomous: true
requirements: [EXP-01, EXP-02, EXP-03]
user_setup: []
must_haves:
  truths:
    - "Both SVG and Excalidraw exporters are importable from the root package entrypoint."
    - "A single committed CoordinatedDiagram fixture generates both SVG and Excalidraw golden outputs."
    - "Full verification passes and exporter geometry recomputation is blocked by tests or static gates."
  artifacts:
    - path: "src/index.ts"
      provides: "Root exporter API"
      contains: "./exporters/index.js"
    - path: "test/fixtures/phase-04/coordinated-export.canonical.json"
      provides: "Shared coordinated source fixture"
    - path: "test/fixtures/phase-04/coordinated-export.svg"
      provides: "SVG golden output"
    - path: "test/fixtures/phase-04/coordinated-export.excalidraw.json"
      provides: "Excalidraw golden output"
    - path: "test/public-api.test.ts"
      provides: "Root import proof for exporter APIs"
      contains: "exportSvg"
  key_links:
    - from: "src/index.ts"
      to: "src/exporters/index.ts"
      via: "root barrel export"
      pattern: "export \\* from \"\\.\\/exporters\\/index\\.js\""
    - from: "test/determinism.test.ts"
      to: "test/fixtures/phase-04/coordinated-export.svg"
      via: "SVG golden comparison"
      pattern: "coordinated-export\\.svg"
    - from: "test/determinism.test.ts"
      to: "test/fixtures/phase-04/coordinated-export.excalidraw.json"
      via: "Excalidraw golden comparison"
      pattern: "coordinated-export\\.excalidraw\\.json"
---

<objective>
Publish Phase 4 exporters through the root API and lock them with shared coordinated fixtures and full gates.

Purpose: Complete EXP-01, EXP-02, and EXP-03 by proving both exporters consume one coordinated IR source and remain deterministic.
Output: root exports, public API tests, Phase 4 fixtures, golden comparisons, and full verification gates.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-coordinated-exporters/04-CONTEXT.md
@.planning/phases/04-coordinated-exporters/04-RESEARCH.md
@.planning/phases/04-coordinated-exporters/04-PATTERNS.md
@.planning/phases/04-coordinated-exporters/04-02-SUMMARY.md
@.planning/phases/04-coordinated-exporters/04-03-SUMMARY.md
@src/index.ts
@test/public-api.test.ts
@test/determinism.test.ts
@biome.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish exporter APIs and public import proof</name>
  <files>src/index.ts, test/public-api.test.ts</files>
  <read_first>src/index.ts, src/exporters/index.ts, test/public-api.test.ts, package.json</read_first>
  <action>Add `export * from "./exporters/index.js";` to `src/index.ts`. Extend `test/public-api.test.ts` to import `exportSvg`, `exportExcalidraw`, and `computeArrowhead` from `../src/index.js`. Add one public API test that builds a small `CoordinatedDiagram`, calls `exportSvg(diagram)`, calls `exportExcalidraw(diagram)`, and asserts the SVG contains `<svg` and the Excalidraw JSON parses with `type === "excalidraw"`. Keep `package.json.exports` root-only as `["."]`.</action>
  <verify>rtk npm test -- public-api exporters</verify>
  <acceptance_criteria>
    - `src/index.ts` contains `export * from "./exporters/index.js"`
    - `test/public-api.test.ts` contains `exportSvg`
    - `test/public-api.test.ts` contains `exportExcalidraw`
    - `test/public-api.test.ts` contains `computeArrowhead`
    - `rtk npm test -- public-api exporters` exits 0
  </acceptance_criteria>
  <done>Exporter APIs are available from the root package entrypoint without package subpath exports.</done>
</task>

<task type="auto">
  <name>Task 2: Add shared Phase 4 coordinated fixtures and golden checks</name>
  <files>test/fixtures/phase-04/coordinated-export.canonical.json, test/fixtures/phase-04/coordinated-export.svg, test/fixtures/phase-04/coordinated-export.excalidraw.json, test/determinism.test.ts, biome.json</files>
  <read_first>test/determinism.test.ts, test/fixtures/phase-03/hybrid-layout.canonical.json, src/serialization/canonical.ts, biome.json</read_first>
  <action>Create `test/fixtures/phase-04/` with one committed coordinated fixture named `coordinated-export.canonical.json`. The fixture must include at least two nodes with labels and `labelLayout`, one group, and one edge with at least two points. Generate or write `coordinated-export.svg` from `exportSvg(fixture)` and `coordinated-export.excalidraw.json` from `exportExcalidraw(fixture)`. Extend `test/determinism.test.ts` or `test/exporters.test.ts` to read the coordinated fixture and assert `exportSvg(fixture)` exactly equals the SVG golden, and `stringifyCanonical(JSON.parse(exportExcalidraw(fixture)))` exactly equals the Excalidraw golden. Update `biome.json` to exclude `test/fixtures/phase-04` if exact fixture formatting is serializer-owned.</action>
  <verify>rtk npm test -- determinism exporters</verify>
  <acceptance_criteria>
    - `test/fixtures/phase-04/coordinated-export.canonical.json` exists and is non-empty
    - `test/fixtures/phase-04/coordinated-export.svg` exists and is non-empty
    - `test/fixtures/phase-04/coordinated-export.excalidraw.json` exists and is non-empty
    - `test/determinism.test.ts` or `test/exporters.test.ts` contains `coordinated-export.svg`
    - `test/determinism.test.ts` or `test/exporters.test.ts` contains `coordinated-export.excalidraw.json`
    - `rtk npm test -- determinism exporters` exits 0
  </acceptance_criteria>
  <done>One coordinated fixture locks both SVG and Excalidraw output.</done>
</task>

<task type="auto">
  <name>Task 3: Add exporter geometry-recompute and full verification gates</name>
  <files>test/exporters.test.ts, .planning/phases/04-coordinated-exporters/04-04-PLAN.md</files>
  <read_first>test/exporters.test.ts, .planning/phases/04-coordinated-exporters/04-CONTEXT.md, package.json</read_first>
  <action>Add a test or static helper in `test/exporters.test.ts` that reads files under `src/exporters/` and fails if they contain any forbidden recomputation imports or calls: `solveDiagram`, `runDagreInitialLayout`, `applyLayoutConstraints`, `routeEdge`, `fitLabel`, `TextMeasurer`, `computeShapeGeometry`, or `computeContainerGeometry`. Run full gates: `rtk npm run typecheck`, `rtk npm test -- exporters public-api determinism`, `rtk npm run lint`, and `rtk npm run verify`. Do not weaken EXP-03 to code review only.</action>
  <verify>rtk npm run verify</verify>
  <acceptance_criteria>
    - `test/exporters.test.ts` contains `solveDiagram`
    - `test/exporters.test.ts` contains `runDagreInitialLayout`
    - `test/exporters.test.ts` contains `computeShapeGeometry`
    - `test/exporters.test.ts` contains `computeContainerGeometry`
    - `rtk npm run verify` exits 0
  </acceptance_criteria>
  <done>Full verification passes and exporter geometry recomputation is automatically blocked.</done>
</task>

</tasks>

<verification>
- [ ] `rtk npm test -- exporters public-api determinism`
- [ ] `rtk npm run typecheck`
- [ ] `rtk npm run lint`
- [ ] `rtk npm run verify`
- [ ] `rtk rg -n 'solveDiagram|runDagreInitialLayout|applyLayoutConstraints|routeEdge|fitLabel|TextMeasurer|computeShapeGeometry|computeContainerGeometry' src/exporters` prints no output
</verification>

<success_criteria>
- Both exporters are root-importable.
- Shared coordinated fixture drives both golden outputs.
- Full project verification passes.
- EXP-03 is enforced by automated tests/static gates.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixture -> golden outputs | Test fixtures become reviewable exporter contracts. |
| root public API -> consumers | Exporter functions become public package surface. |

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-04-10 | Tampering | `test/fixtures/phase-04/*` | mitigate | Golden fixtures are generated from one coordinated source and compared byte-for-byte/canonically. |
| T-04-11 | Tampering | `src/index.ts` | mitigate | Public API test proves root import; package exports remain root-only. |
| T-04-12 | Denial of Service | verification gates | mitigate | Full `rtk npm run verify` remains the final gate; no external services are required. |
</threat_model>

<output>
After completion, create `.planning/phases/04-coordinated-exporters/04-04-SUMMARY.md`
</output>
