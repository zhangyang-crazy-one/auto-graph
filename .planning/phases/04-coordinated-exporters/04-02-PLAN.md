---
phase: 04-coordinated-exporters
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/exporters/svg.ts
  - src/exporters/index.ts
  - test/exporters.test.ts
autonomous: true
requirements: [EXP-01, EXP-03]
user_setup: []
must_haves:
  truths:
    - "SVG exporter writes a standalone SVG with viewBox derived from coordinated bounds."
    - "SVG exporter renders seven existing node shapes without recomputing shape geometry."
    - "SVG exporter renders labels from labelLayout lines when available and does not call text measurement."
    - "SVG exporter renders groups, edge paths, and vector arrowheads from coordinated boxes and points."
  artifacts:
    - path: "src/exporters/svg.ts"
      provides: "Standalone SVG exporter"
      exports: ["exportSvg"]
      contains: "<svg"
    - path: "test/exporters.test.ts"
      provides: "SVG exporter behavioral coverage"
      contains: "exportSvg"
  key_links:
    - from: "src/exporters/svg.ts"
      to: "src/exporters/arrow.ts"
      via: "arrowhead helper"
      pattern: "computeArrowhead"
    - from: "src/exporters/svg.ts"
      to: "src/ir/diagram.ts"
      via: "CoordinatedDiagram input"
      pattern: "CoordinatedDiagram"
---

<objective>
Implement the standalone SVG exporter from coordinated IR.

Purpose: SVG is the first concrete proof that the solved coordinated geometry can render without duplicate geometry logic.
Output: `exportSvg()` plus SVG tests for shapes, labels, groups, edges, viewBox, and arrowheads.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-coordinated-exporters/04-CONTEXT.md
@.planning/phases/04-coordinated-exporters/04-RESEARCH.md
@.planning/phases/04-coordinated-exporters/04-PATTERNS.md
@.planning/phases/04-coordinated-exporters/04-01-SUMMARY.md
@src/exporters/types.ts
@src/exporters/arrow.ts
@src/ir/diagram.ts
@src/ir/elements.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement SVG shape, group, and label serialization</name>
  <files>src/exporters/svg.ts, src/exporters/index.ts, test/exporters.test.ts</files>
  <read_first>src/exporters/types.ts, src/ir/elements.ts, src/labels/types.ts, diagram-geometry-engine-design.md</read_first>
  <action>Create `src/exporters/svg.ts` exporting `exportSvg(diagram: CoordinatedDiagram, options: ExportOptions = {}): string`. Return a complete SVG string with XML namespace, `role="img"`, optional `<title>`, `viewBox="{bounds.x} {bounds.y} {bounds.width} {bounds.height}"`, white background, neutral default styles, group boxes before nodes, nodes before labels, and edges with arrowheads. Implement shape serialization for: rectangle as `<rect>`, rounded-rectangle as `<rect rx="8" ry="8">`, ellipse as `<ellipse>`, diamond/parallelogram/hexagon as `<polygon points="...">`, and cylinder as deterministic `<path>` or grouped paths. Labels must render from `labelLayout.lines` when present, using each line's `box.x`, `baselineY`, and `text`; if only `label.text` exists, render a single centered text fallback without measuring. Escape XML special characters in text and attributes. Add `export * from "./svg.js";` to `src/exporters/index.ts`.</action>
  <verify>rtk npm test -- exporters</verify>
  <acceptance_criteria>
    - `src/exporters/svg.ts` contains `export function exportSvg`
    - `src/exporters/svg.ts` contains `viewBox=`
    - `src/exporters/svg.ts` contains `rounded-rectangle`
    - `src/exporters/svg.ts` contains `cylinder`
    - `src/exporters/svg.ts` contains `labelLayout.lines`
    - `src/exporters/index.ts` contains `export * from "./svg.js"`
  </acceptance_criteria>
  <done>SVG nodes, groups, and labels serialize deterministically from coordinated IR.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SVG edge paths and vector arrowheads</name>
  <files>src/exporters/svg.ts, test/exporters.test.ts</files>
  <read_first>src/exporters/arrow.ts, src/routing/routes.ts, .planning/phases/04-coordinated-exporters/04-CONTEXT.md</read_first>
  <action>In `exportSvg()`, render each `CoordinatedEdge.points` as a `<path>` with a stable `d="M ... L ..."` sequence. Use `computeArrowhead(edge.points)` to append an explicit arrowhead `<polygon>` whose tip equals the final edge point and whose base points come from the helper. Include an SVG `<defs>` marker section only if desired for standalone compatibility, but tests must assert vector polygon output rather than relying only on `marker-end`. Do not import `routeEdge`, solver, layout, text, labels, or geometry modules in `src/exporters/svg.ts`.</action>
  <verify>rtk npm test -- exporters</verify>
  <acceptance_criteria>
    - `src/exporters/svg.ts` contains `computeArrowhead`
    - `src/exporters/svg.ts` contains `<path`
    - `src/exporters/svg.ts` contains `<polygon`
    - `test/exporters.test.ts` contains `arrowhead`
    - `rtk rg -n 'routeEdge|solveDiagram|runDagreInitialLayout|fitLabel|TextMeasurer|computeShapeGeometry|computeContainerGeometry' src/exporters/svg.ts` prints no output
  </acceptance_criteria>
  <done>SVG edges and vector arrowheads are exported from coordinated points without routing recomputation.</done>
</task>

</tasks>

<verification>
- [ ] `rtk npm test -- exporters`
- [ ] `rtk npm run typecheck`
- [ ] `rtk rg -n 'routeEdge|solveDiagram|runDagreInitialLayout|fitLabel|TextMeasurer|computeShapeGeometry|computeContainerGeometry' src/exporters/svg.ts` prints no output
</verification>

<success_criteria>
- SVG is standalone, deterministic, and includes shapes, labels, groups, edges, and arrowheads.
- SVG exporter consumes coordinated fields only.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coordinated labels -> SVG text | User text becomes XML text nodes and attributes. |
| coordinated points -> SVG path | Edge geometry becomes path and arrowhead coordinates. |

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-04-04 | Tampering | `src/exporters/svg.ts` | mitigate | Escape XML text/attributes and render only finite coordinated numbers. |
| T-04-05 | Tampering | `src/exporters/svg.ts` | mitigate | Static grep prevents imports from solver/layout/text/routing/geometry recomputation modules. |
| T-04-06 | Spoofing | SVG arrowheads | mitigate | Compute arrowhead direction and tip from final edge vector for deterministic semantic direction. |
</threat_model>

<output>
After completion, create `.planning/phases/04-coordinated-exporters/04-02-SUMMARY.md`
</output>
