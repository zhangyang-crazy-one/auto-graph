---
phase: 04-coordinated-exporters
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/exporters/excalidraw.ts
  - src/exporters/index.ts
  - test/exporters.test.ts
autonomous: true
requirements: [EXP-02, EXP-03]
user_setup: []
must_haves:
  truths:
    - "Excalidraw exporter produces deterministic JSON from coordinated IR."
    - "Excalidraw output has separate editable shape and text elements."
    - "Excalidraw arrows derive from coordinated edge points and include start/end bindings."
    - "Excalidraw adapter does not become a core IR or solver dependency."
  artifacts:
    - path: "src/exporters/excalidraw.ts"
      provides: "Excalidraw JSON exporter adapter"
      exports: ["exportExcalidraw"]
      contains: "startBinding"
    - path: "test/exporters.test.ts"
      provides: "Excalidraw exporter tests"
      contains: "exportExcalidraw"
  key_links:
    - from: "src/exporters/excalidraw.ts"
      to: "src/ir/diagram.ts"
      via: "CoordinatedDiagram input"
      pattern: "CoordinatedDiagram"
    - from: "src/exporters/index.ts"
      to: "src/exporters/excalidraw.ts"
      via: "barrel export"
      pattern: "export \\* from \"\\.\\/excalidraw\\.js\""
---

<objective>
Implement Excalidraw as a Phase 4 exporter attachment/adapter.

Purpose: Prove coordinated IR can drive an editable JSON diagram format while keeping Excalidraw-specific semantics out of core IR and solver logic.
Output: `exportExcalidraw()` plus deterministic tests for elements, text, arrow bindings, and groups.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-coordinated-exporters/04-CONTEXT.md
@.planning/phases/04-coordinated-exporters/04-RESEARCH.md
@.planning/phases/04-coordinated-exporters/04-PATTERNS.md
@.planning/phases/04-coordinated-exporters/04-01-SUMMARY.md
@src/exporters/types.ts
@src/ir/diagram.ts
@src/ir/elements.ts
@src/serialization/canonical.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement deterministic Excalidraw scene export</name>
  <files>src/exporters/excalidraw.ts, src/exporters/index.ts, test/exporters.test.ts</files>
  <read_first>src/exporters/types.ts, src/ir/diagram.ts, src/ir/elements.ts, .planning/phases/04-coordinated-exporters/04-CONTEXT.md</read_first>
  <action>Create `src/exporters/excalidraw.ts` exporting `exportExcalidraw(diagram: CoordinatedDiagram, options: ExportOptions = {}): string`. Return a JSON string for a deterministic Excalidraw-compatible scene object with top-level fields `type: "excalidraw"`, `version: 2`, `source: "diagram-geometry-engine"`, `elements`, `appState`, and `files: {}`. Generate deterministic element IDs using prefixes like `node:${id}`, `node-text:${id}`, `edge:${id}`, `group:${id}`, and `group-text:${id}`. Export node/group shapes and separate text elements. Use neutral engineering defaults, not Excalidraw hand-drawn style. Add `export * from "./excalidraw.js";` to `src/exporters/index.ts`.</action>
  <verify>rtk npm test -- exporters</verify>
  <acceptance_criteria>
    - `src/exporters/excalidraw.ts` contains `export function exportExcalidraw`
    - `src/exporters/excalidraw.ts` contains `type: "excalidraw"`
    - `src/exporters/excalidraw.ts` contains `source: "diagram-geometry-engine"`
    - `src/exporters/excalidraw.ts` contains `node-text:`
    - `src/exporters/index.ts` contains `export * from "./excalidraw.js"`
  </acceptance_criteria>
  <done>Excalidraw scene JSON exports deterministic editable node/group/text elements.</done>
</task>

<task type="auto">
  <name>Task 2: Add arrow bindings and group relationships</name>
  <files>src/exporters/excalidraw.ts, test/exporters.test.ts</files>
  <read_first>src/ir/elements.ts, test/exporters.test.ts, .planning/phases/04-coordinated-exporters/04-CONTEXT.md</read_first>
  <action>Represent each coordinated edge as an Excalidraw arrow element whose `points` are relative to the first point and whose absolute `x`/`y` equal the first coordinated point. Include `startBinding` with `elementId: "node:${source.nodeId}"` and `endBinding` with `elementId: "node:${target.nodeId}"`. For groups, include visible group boundary rectangle elements and assign deterministic `groupIds` arrays to children where practical. If a group relationship cannot be represented exactly without a full Excalidraw dependency, preserve deterministic boundary elements and child `groupIds` in the adapter-local JSON shape. Do not import solver/layout/text/routing/geometry modules.</action>
  <verify>rtk npm test -- exporters</verify>
  <acceptance_criteria>
    - `src/exporters/excalidraw.ts` contains `startBinding`
    - `src/exporters/excalidraw.ts` contains `endBinding`
    - `src/exporters/excalidraw.ts` contains `groupIds`
    - `test/exporters.test.ts` contains `startBinding`
    - `test/exporters.test.ts` contains `groupIds`
    - `rtk rg -n 'routeEdge|solveDiagram|runDagreInitialLayout|fitLabel|TextMeasurer|computeShapeGeometry|computeContainerGeometry' src/exporters/excalidraw.ts` prints no output
  </acceptance_criteria>
  <done>Excalidraw arrows and groups preserve edit semantics from coordinated IR.</done>
</task>

</tasks>

<verification>
- [ ] `rtk npm test -- exporters`
- [ ] `rtk npm test -- public-api`
- [ ] `rtk npm run typecheck`
- [ ] `rtk rg -n 'routeEdge|solveDiagram|runDagreInitialLayout|fitLabel|TextMeasurer|computeShapeGeometry|computeContainerGeometry' src/exporters/excalidraw.ts` prints no output
</verification>

<success_criteria>
- Excalidraw JSON is deterministic and editable enough for Phase 4.
- Excalidraw stays an exporter adapter and does not affect core IR semantics.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coordinated IR -> Excalidraw JSON | Internal geometry becomes editable external JSON format. |
| edge endpoints -> Excalidraw bindings | Node IDs become binding references. |

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-04-07 | Tampering | `src/exporters/excalidraw.ts` | mitigate | Deterministic IDs derive from coordinated IDs; no random IDs. |
| T-04-08 | Tampering | Excalidraw adapter | mitigate | Static grep prevents solver/layout/text/routing/geometry recomputation imports. |
| T-04-09 | Repudiation | Excalidraw output | mitigate | Golden canonical JSON fixtures make output changes reviewable. |
</threat_model>

<output>
After completion, create `.planning/phases/04-coordinated-exporters/04-03-SUMMARY.md`
</output>
