# Phase 11 Plan 01: Rails, Gutters, And Post-growth Repair

## Goal

Promote page-level rail/gutter allocation and close PR #72 review risks in rail and anchor-capacity behavior.

## Tasks

1. Add page-level `routing.rails` and `routing.gutters` output.
2. Validate rails against all non-endpoint node obstacles by identity.
3. Avoid framed title/header bands for LR/RL rails.
4. Skip incompatible explicit anchors for rail fast paths.
5. Move anchor-capacity growth after swimlane contracts and avoid caller mutation.
6. Report post-growth overlaps as deliverability blockers.
7. Align routing fallback hard-clear checks with final collision contract.

## Verification

- `npm test -- test/solver.test.ts test/dense-acceptance.test.ts`
- `npm run typecheck`
