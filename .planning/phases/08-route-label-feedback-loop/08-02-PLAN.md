---
phase: 08-route-label-feedback-loop
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/solver/solve.ts
  - test/solver.test.ts
  - test/determinism.test.ts
autonomous: true
requirements:
  - LOOP-02
  - LOOP-03
  - LOOP-04
must_haves:
  truths:
    - D-02: current final text obstacles still exclude connected text through existing helper behavior.
    - D-09: no reroute candidate is accepted if it worsens conflict, obstacle, or backtracking score.
    - D-10: explicit `edgeLabelRerouting` controls the deterministic feedback budget.
    - D-11: stable edge-id ordering keeps repeated output stable.
    - D-12: candidate scoring is deterministic and objective.
    - D-13: feedback stops when no improvements are possible.
    - PR review `discussion_r3541568275`: explicit label rerouting must be honored for orthogonal routes when supported.
  artifacts:
    - `edgeLabelRerouteIterations` honors explicit orthogonal rerouting.
    - `test/solver.test.ts` covers explicit orthogonal rerouting behavior.
    - `test/determinism.test.ts` covers repeated feedback-loop output stability.
  key_links:
    - Plan 02 depends on Plan 01 feedback-loop implementation.
    - Focused tests run before the full `npm run verify` gate.
---

<objective>
Lock Phase 8 determinism and compatibility behavior around the new feedback loop.

Purpose: Make the new route/label loop stable across repeated runs and honor explicit rerouting options used by callers that keep default orthogonal routing.
Output: Orthogonal rerouting compatibility, feedback-loop determinism coverage, and full verification.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-route-label-feedback-loop/08-CONTEXT.md
@.planning/phases/08-route-label-feedback-loop/08-RESEARCH.md
@.planning/phases/08-route-label-feedback-loop/08-VALIDATION.md
@.planning/phases/08-route-label-feedback-loop/08-PATTERNS.md
@.planning/phases/08-route-label-feedback-loop/08-01-PLAN.md
@src/solver/solve.ts
@test/solver.test.ts
@test/determinism.test.ts
</context>

<threat_model>
ASVS level: 1
Block on: high

- T-08-05 silent option ignore: mitigate by testing explicit `edgeLabelRerouting: true` for orthogonal routing.
- T-08-06 output instability: mitigate with a canonical repeated-solve determinism test that exercises accepted reroutes.
- T-08-07 stale diagnostics after rerouting: mitigate with focused solver assertions and the full `npm run verify` suite.

No high security threats are expected; this plan modifies deterministic geometry behavior only.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Honor explicit orthogonal route/label rerouting</name>
  <files>src/solver/solve.ts, test/solver.test.ts</files>
  <read_first>src/solver/solve.ts, test/solver.test.ts, .planning/phases/08-route-label-feedback-loop/08-RESEARCH.md</read_first>
  <action>Update `edgeLabelRerouteIterations(options)` so explicit `edgeLabelRerouting: true` and explicit `edgeLabelRerouting: { maxIterations: N }` return a positive bounded iteration count for supported `routeKind` values `orthogonal` and `obstacle-avoiding`. Preserve `edgeLabelRerouting: false` as zero. Preserve the existing default behavior where `routeKind: "obstacle-avoiding"` gets the default feedback budget and default orthogonal routing does not opt in unless `edgeLabelRerouting` is explicitly set. Add a solver regression proving explicit orthogonal rerouting performs a second pass and can clear a final route/text conflict or, if impossible, emits `routing.route-label-loop.exhausted` instead of silently ignoring the option.</action>
  <verify>rtk npm test -- test/solver.test.ts</verify>
  <acceptance_criteria>
    - `edgeLabelRerouteIterations` returns 0 for `edgeLabelRerouting: false`.
    - `edgeLabelRerouteIterations` returns 4 for explicit `edgeLabelRerouting: true` with `routeKind: "orthogonal"`.
    - `edgeLabelRerouteIterations` returns `Math.floor(maxIterations)` for explicit object settings with `routeKind: "orthogonal"`.
    - Default orthogonal solves do not unexpectedly enable the feedback budget when `edgeLabelRerouting` is undefined.
    - `test/solver.test.ts` contains a regression tied to PR review `discussion_r3541568275`.
    - `rtk npm test -- test/solver.test.ts` exits 0.
  </acceptance_criteria>
  <done>Explicit orthogonal label rerouting is honored and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Add feedback-loop determinism coverage</name>
  <files>test/determinism.test.ts, test/solver.test.ts</files>
  <read_first>test/determinism.test.ts, test/solver.test.ts</read_first>
  <action>Add a deterministic feedback-loop fixture to `test/determinism.test.ts` that solves the same diagram twice with `edgeLabelRerouting` enabled and compares `stringifyCanonical(solveDiagram(input, options))` byte-for-byte. The fixture must exercise the feedback loop by including a final text conflict that Plan 01 clears or exhausts. Reuse local helper style from `test/determinism.test.ts`; do not introduce snapshot files unless the test needs a committed golden.</action>
  <verify>rtk npm test -- test/determinism.test.ts test/solver.test.ts</verify>
  <acceptance_criteria>
    - `test/determinism.test.ts` includes a repeated solve assertion for a feedback-loop fixture.
    - The fixture uses `edgeLabelRerouting` and `routeKind` settings that make the Phase 8 loop run.
    - The assertion compares canonical serialized full solver output, not only route points.
    - `rtk npm test -- test/determinism.test.ts test/solver.test.ts` exits 0.
  </acceptance_criteria>
  <done>Feedback-loop output stability is covered.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 8 verification and inspect diagnostic surface</name>
  <files>src/solver/solve.ts, src/ir/diagnostics.ts, test/solver.test.ts, test/determinism.test.ts</files>
  <read_first>src/solver/solve.ts, src/ir/diagnostics.ts, test/solver.test.ts, test/determinism.test.ts, .planning/phases/08-route-label-feedback-loop/08-VALIDATION.md</read_first>
  <action>Run the focused tests, run full verification, and inspect the source for the exact expected diagnostic and helper identifiers. If `npm run verify` fails due to Phase 8 changes, fix the source or tests before creating the summary. Do not weaken tests by removing route/text assertions. Record final command results in `08-02-SUMMARY.md` after execution.</action>
  <verify>rtk npm test -- test/solver.test.ts test/determinism.test.ts && rtk npm run verify</verify>
  <acceptance_criteria>
    - `rtk npm test -- test/solver.test.ts test/determinism.test.ts` exits 0.
    - `rtk npm run verify` exits 0.
    - `rtk rg -n "routing.route-label-loop.exhausted" src test` finds solver, diagnostics, and test references.
    - Final summaries record command results and any intentional deviations.
  </acceptance_criteria>
  <done>Phase 8 is ready for verification with focused and full test coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rtk npm test -- test/solver.test.ts test/determinism.test.ts`
- [ ] `rtk npm run verify`
- [ ] `rtk rg -n "routing.route-label-loop.exhausted|edgeLabelRerouteIterations|discussion_r3541568275" src test .planning/phases/08-route-label-feedback-loop`
</verification>

<success_criteria>
- Explicit orthogonal route/label feedback is not silently ignored.
- Feedback-loop output is deterministic across repeated solves.
- Full project verification passes after Phase 8 implementation.
- Later strict status, external labels, and rail/gutter architecture remain deferred to Phases 9, 10, and 11.
</success_criteria>

<output>
After completion, create `.planning/phases/08-route-label-feedback-loop/08-02-SUMMARY.md`.
</output>
