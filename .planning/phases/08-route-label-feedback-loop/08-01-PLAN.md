---
phase: 08-route-label-feedback-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/solver/solve.ts
  - src/ir/diagnostics.ts
  - test/solver.test.ts
autonomous: true
requirements:
  - LOOP-01
  - LOOP-02
  - LOOP-04
must_haves:
  truths:
    - D-01: the feedback loop considers all final `isRouteClearanceText` conflicts, not only `edge-label`.
    - D-02: rerouting text obstacles exclude only text connected to the rerouted edge through `isEdgeConnectedTextAnnotation`.
    - D-03: Phase 8 keeps public strict/deliverable semantics out of scope.
    - D-04: route/label feedback uses a private internal state object instead of scattered local mutation.
    - D-05: the feedback state stays private to solver implementation.
    - D-06: prepare/solve/export separation remains unchanged.
    - D-07: bounded loop exhaustion emits `routing.route-label-loop.exhausted` with structured details.
    - D-08: existing `routing.text-clearance.unresolved` diagnostics remain.
    - D-09: candidate reroutes are rejected when they worsen conflicts, obstacle diagnostics, or backtracking.
    - D-10: the loop budget comes from `edgeLabelRerouting`, defaulting to four iterations where enabled.
    - D-11: conflicting edges are processed in stable edge-id order and labels are recomputed after accepted changes.
    - D-12: candidate acceptance uses deterministic scoring before route length and bend tie-breakers.
    - D-13: the loop stops early when no improvements are accepted or no route/text conflicts remain.
  artifacts:
    - `src/solver/solve.ts` contains a private route/label feedback loop.
    - `src/ir/diagnostics.ts` includes `routing.route-label-loop.exhausted` in deliverability diagnostics.
    - `test/solver.test.ts` covers node-label feedback rerouting, edge-label feedback rerouting, and exhausted-loop diagnostics.
  key_links:
    - `coordinateEdges` remains the only path used to reroute edges.
    - `coordinateEdgeTextAnnotations` remains the final edge-label placement path.
    - `reportRouteTextClearance` remains the final conflict oracle.
---

<objective>
Implement the core closed-loop route/label feedback pass for Phase 8.

Purpose: Let the solver reroute final edge routes against final route-clearance text geometry instead of reporting all conflicts only after final label placement.
Output: Private solver feedback loop, structured exhausted-loop diagnostic, compatibility diagnostics, and focused solver regressions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-route-label-feedback-loop/08-CONTEXT.md
@.planning/phases/08-route-label-feedback-loop/08-RESEARCH.md
@.planning/phases/08-route-label-feedback-loop/08-VALIDATION.md
@.planning/phases/08-route-label-feedback-loop/08-PATTERNS.md
@src/solver/solve.ts
@src/ir/diagnostics.ts
@test/solver.test.ts
</context>

<threat_model>
ASVS level: 1
Block on: high

- T-08-01 unbounded loop: mitigate with `edgeLabelRerouteIterations(options)`, a fixed iteration count, stable edge order, and early stop when an iteration accepts no candidates.
- T-08-02 nondeterministic output: mitigate with sorted edge ids, sorted diagnostic detail lists, and strict candidate improvement checks.
- T-08-03 degraded geometry hidden as success: mitigate by preserving `routing.text-clearance.unresolved` diagnostics and adding `routing.route-label-loop.exhausted` to deliverability diagnostics.
- T-08-04 public API drift: mitigate by keeping feedback state private and preserving `SolveDiagramOptions.edgeLabelRerouting`.

No high security threats are expected; this is a headless in-memory TypeScript library change with no server, database, credentials, or network IO.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add private feedback state and score helpers</name>
  <files>src/solver/solve.ts</files>
  <read_first>src/solver/solve.ts, .planning/phases/08-route-label-feedback-loop/08-CONTEXT.md, .planning/phases/08-route-label-feedback-loop/08-PATTERNS.md</read_first>
  <action>Add private solver-only types and helpers near the existing edge-label reroute code: `RouteLabelFeedbackState`, `RouteLabelFeedbackScore`, `routeLabelFeedbackTextAnnotations`, `routeLabelFeedbackConflicts`, `edgeIdsFromRouteTextDiagnostics`, `scoreRouteLabelFeedbackCandidate`, `compareRouteLabelFeedbackScore`, and local route-length/bend tie-break helpers. The score must order by final route/text conflicts for the edge, route obstacle diagnostics for that edge, `routing.backtracking_excessive` diagnostics for that edge, route length, and bend count. Do not export these types or helpers.</action>
  <verify>rtk npm run typecheck</verify>
  <acceptance_criteria>
    - `src/solver/solve.ts` contains `RouteLabelFeedbackState` and `RouteLabelFeedbackScore`.
    - `src/solver/solve.ts` contains `scoreRouteLabelFeedbackCandidate`.
    - The helper score compares final route/text conflicts before route length and bend count.
    - No new public export is added for feedback state or score helpers.
    - `rtk npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Private helper state compiles without changing public API.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the edge-label-only reroute loop with full route/text feedback</name>
  <files>src/solver/solve.ts, src/ir/diagnostics.ts</files>
  <read_first>src/solver/solve.ts, src/ir/diagnostics.ts, .planning/phases/08-route-label-feedback-loop/08-RESEARCH.md</read_first>
  <action>Replace the existing loop that filters `reportRouteTextClearance(...edgeTextAnnotations...)` down to `textSurfaceKind === "edge-label"` with a bounded feedback loop over `routeLabelFeedbackTextAnnotations(baseTextAnnotations, frameTextAnnotation, edgeTextAnnotations)`. In each iteration, compute all conflicts with `reportRouteTextClearance`, sort conflicting edge ids lexicographically, reroute one conflicting edge at a time through `coordinateEdges` with all current final text annotations as text obstacles, recompute edge labels after accepted changes, and accept only if `compareRouteLabelFeedbackScore(candidate, current) < 0`. Remove stale routing diagnostics for accepted edge ids before appending candidate diagnostics. When conflicts remain after the loop stops, push `routing.route-label-loop.exhausted` with `iterations`, `maxIterations`, `conflictCount`, sorted `edgeIds`, sorted `ownerIds`, sorted `textSurfaceKinds`, `acceptedReroutes`, `rejectedReroutes`, and `suggestedRemedy`. Add `routing.route-label-loop.exhausted` to `DELIVERABILITY_DIAGNOSTIC_CODES`.</action>
  <verify>rtk npm run typecheck</verify>
  <acceptance_criteria>
    - The old `textSurfaceKind === "edge-label"` loop filter is removed or no longer controls the feedback loop scope.
    - Feedback conflicts are computed from final base, frame, and edge text annotations.
    - Rerouted candidates are produced through `coordinateEdges`, not a new router path.
    - Accepted reroutes cause `coordinateEdgeTextAnnotations` to run again before the next conflict check.
    - Candidate acceptance rejects non-improving or worsening scores.
    - `routing.route-label-loop.exhausted` is emitted only when final route/text conflicts remain after bounded feedback stops.
    - `src/ir/diagnostics.ts` includes `routing.route-label-loop.exhausted` in `DELIVERABILITY_DIAGNOSTIC_CODES`.
    - `rtk npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>The solver has a bounded full route/text feedback loop with structured exhaustion diagnostics.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused solver regressions for feedback and exhaustion</name>
  <files>test/solver.test.ts, src/solver/solve.ts, src/ir/diagnostics.ts</files>
  <read_first>test/solver.test.ts, src/solver/solve.ts, src/ir/diagnostics.ts</read_first>
  <action>Add Vitest coverage in `test/solver.test.ts` for three behaviors. First, a final `node-label` route/text conflict in an obstacle-avoiding solve is cleared by the feedback loop, with no final `routing.text-clearance.unresolved` for that `node-label`. Second, a final `edge-label` route/text conflict is cleared by the same loop without relying only on dry-run estimates. Third, an impossible huge-label or blocked-corridor fixture with `edgeLabelRerouting: { maxIterations: 1 }` emits `routing.route-label-loop.exhausted` and still emits at least one compatibility `routing.text-clearance.unresolved`. Update the strict diagnostic certification list to include `routing.route-label-loop.exhausted`.</action>
  <verify>rtk npm test -- test/solver.test.ts</verify>
  <acceptance_criteria>
    - `test/solver.test.ts` has a regression asserting final `node-label` conflict clearance.
    - `test/solver.test.ts` has a regression asserting final `edge-label` conflict clearance.
    - `test/solver.test.ts` has a regression asserting `routing.route-label-loop.exhausted` on a bounded impossible case.
    - The exhausted-loop test also asserts compatibility `routing.text-clearance.unresolved` remains.
    - The deliverability diagnostic certification list includes `routing.route-label-loop.exhausted`.
    - `rtk npm test -- test/solver.test.ts` exits 0.
  </acceptance_criteria>
  <done>Focused solver regressions prove LOOP-01, LOOP-02, and LOOP-04.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rtk npm run typecheck`
- [ ] `rtk npm test -- test/solver.test.ts`
- [ ] `rtk rg -n "route-label-loop.exhausted|RouteLabelFeedback|scoreRouteLabelFeedback|edgeLabelRerouting" src/solver/solve.ts src/ir/diagnostics.ts test/solver.test.ts`
</verification>

<success_criteria>
- Solver validates final routes against final route-clearance text geometry after final edge-label placement.
- Conflicting edges can be rerouted with final text boxes as obstacles within a deterministic budget.
- Remaining conflicts produce `routing.route-label-loop.exhausted` plus compatibility per-conflict diagnostics.
- Strict mode treats the exhausted-loop diagnostic as deliverability-breaking through the existing diagnostic set.
</success_criteria>

<output>
After completion, create `.planning/phases/08-route-label-feedback-loop/08-01-SUMMARY.md`.
</output>
