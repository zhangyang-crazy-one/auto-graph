---
phase: 13
plan: 01
type: implementation
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13-unified-issue-research-and-regression-guards/13-SUMMARY.md
  - test/solver.test.ts
  - test/dense-acceptance.test.ts
autonomous: true
requirements:
  - EPIC-01
  - EPIC-02
  - REG-74-01
  - REG-74-02
---

# Phase 13 Plan 01: Unified Issue Research And Regression Guards

<objective>
Consolidate the current non-polar issue set under #75 and add regression guards so later remediation work cannot reintroduce #74 or route-label feedback hard-route scoring regressions.
</objective>

<threat_model>
No network or credential handling changes. Primary risks are false confidence from weak fixtures and accidental regression of strict deliverability semantics.
</threat_model>

<tasks>
<task id="T13-01" type="execute">
  <read_first>
    - `.planning/PROJECT.md`
    - `.planning/REQUIREMENTS.md`
    - `.planning/ROADMAP.md`
    - `.planning/phases/13-unified-issue-research-and-regression-guards/13-RESEARCH.md`
  </read_first>
  <action>Create `13-SUMMARY.md` after implementation that records the issue routing: #75 active, #69/#71/#73 folded, #74 guarded, #15 excluded. Include the 0.2.17 Stage 5 baseline `132 critical / 179 warnings` and mention that this phase did not implement remediation execution.</action>
  <acceptance_criteria>
    - `13-SUMMARY.md` contains the strings `#75`, `#69`, `#71`, `#73`, `#74`, and `#15`.
    - `13-SUMMARY.md` contains `132 critical / 179 warnings`.
  </acceptance_criteria>
</task>

<task id="T13-02" type="tdd">
  <read_first>
    - `test/solver.test.ts`
    - `src/solver/solve.ts`
    - `src/routing/types.ts`
    - `src/routing/routes.ts`
  </read_first>
  <action>Add or strengthen a targeted regression in `test/solver.test.ts` for #74: route-label feedback text hard obstacles must not produce a fatal `routing.evidence.crossing_forbidden` diagnostic for the affected edge, and `solveDiagramSafe` must return a solved diagram with warning-level text congestion diagnostics when the route remains congested.</action>
  <acceptance_criteria>
    - The test fails if any diagnostic for the fixture edge has `severity: "error"` and `code: "routing.evidence.crossing_forbidden"`.
    - The test asserts the fixture still reports a text-specific degraded diagnostic such as `routing.text-clearance.unresolved` or `routing.route-label-loop.exhausted`.
    - `rtk npm test -- test/solver.test.ts` exits 0.
  </acceptance_criteria>
</task>

<task id="T13-03" type="tdd">
  <read_first>
    - `test/solver.test.ts`
    - `src/solver/solve.ts`
    - `src/routing/routes.ts`
  </read_first>
  <action>Add a route-label feedback scoring guard proving that a reroute candidate that introduces a hard-route diagnostic is rejected even if it reduces route/text conflict count. Use the existing `routing.evidence.crossing_forbidden`, `route_obstacle_fallback`, or text-hard-obstacle fixture patterns; do not add a new public test hook unless a black-box fixture cannot prove the behavior.</action>
  <acceptance_criteria>
    - The accepted final route does not include the hard-route diagnostic introduced by the rejected candidate.
    - The final diagnostics retain structured text congestion or loop-exhausted output when no hard-clear improvement exists.
    - `rtk npm test -- test/solver.test.ts` exits 0.
  </acceptance_criteria>
</task>

<task id="T13-04" type="execute">
  <read_first>
    - `test/dense-acceptance.test.ts`
    - `.planning/REQUIREMENTS.md`
  </read_first>
  <action>Add a lightweight dense acceptance assertion, if not already present, that future dense remediation tests must keep #74 fixed. The assertion should check that dense strict/safe scenarios do not contain fatal evidence-crossing diagnostics caused by text hard obstacles.</action>
  <acceptance_criteria>
    - `test/dense-acceptance.test.ts` has at least one assertion that rejects `routing.evidence.crossing_forbidden` when the conflict source is text.
    - `rtk npm test -- test/dense-acceptance.test.ts test/solver.test.ts` exits 0.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- `rtk npm test -- test/solver.test.ts test/dense-acceptance.test.ts`
- `rtk npm run typecheck`
</verification>

<success_criteria>
- #75 is the only active dense-deliverability epic in planning artifacts.
- #15 remains excluded.
- #74 is guarded by tests before broader remediation execution begins.
- No source change stages or reverts the unrelated `package.json` version bump.
</success_criteria>
