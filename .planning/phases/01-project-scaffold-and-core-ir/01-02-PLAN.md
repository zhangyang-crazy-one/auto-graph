---
phase: 01-project-scaffold-and-core-ir
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/index.ts
  - src/ir/geometry.ts
  - src/ir/elements.ts
  - src/ir/constraints.ts
  - src/ir/diagnostics.ts
  - src/ir/diagram.ts
  - src/ir/index.ts
  - test/public-api.test.ts
autonomous: true
requirements:
  - FND-02
must_haves:
  truths:
    - "Developer can import stable public types from the package entrypoint."
    - "Intent, normalized, and coordinated IR are distinct contracts for the future prepare/solve/export flow."
    - "Core IR does not include renderer-native SVG, Excalidraw, draw.io, Mermaid, HTML, or CSS fields."
  artifacts:
    - path: "src/index.ts"
      provides: "public package entrypoint"
      contains: 'export * from "./ir/index.js";'
    - path: "src/ir/geometry.ts"
      provides: "Point, Size, Box, Insets, DiagramDirection"
      exports: ["Point", "Size", "Box", "Insets", "DiagramDirection"]
    - path: "src/ir/elements.ts"
      provides: "node, edge, group, label, anchor contracts"
      exports: ["IntentNode", "NormalizedNode", "CoordinatedNode", "IntentEdge", "NormalizedEdge", "CoordinatedEdge", "Label"]
    - path: "src/ir/constraints.ts"
      provides: "exact, relative, align, distribute, containment constraint unions"
      exports: ["Constraint"]
    - path: "src/ir/diagnostics.ts"
      provides: "Diagnostic severity/code/path/message contract"
      exports: ["Diagnostic"]
    - path: "src/ir/diagram.ts"
      provides: "IntentDiagram, NormalizedDiagram, CoordinatedDiagram"
      exports: ["IntentDiagram", "NormalizedDiagram", "CoordinatedDiagram"]
    - path: "test/public-api.test.ts"
      provides: "public entrypoint import proof"
  key_links:
    - from: "src/index.ts"
      to: "src/ir/index.ts"
      via: "public re-export"
      pattern: 'export \\* from "./ir/index\\.js";'
    - from: "test/public-api.test.ts"
      to: "src/index.ts"
      via: "public API import"
      pattern: 'from "../src/index\\.js"'
---

<objective>
Create the public TypeScript IR contracts for DGE's Phase 1 API.

Purpose: This plan satisfies FND-02 and establishes the stable type boundary that later prepare, solve, layout, routing, and exporter modules must consume without renderer creep.
Output: Geometry primitives, element contracts, constraint unions, diagnostics, diagram-level IR types, IR barrel exports, package entrypoint exports, and a public API test.
</objective>

<execution_context>
@/home/zhangyangrui/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-project-scaffold-and-core-ir/01-CONTEXT.md
@.planning/phases/01-project-scaffold-and-core-ir/01-RESEARCH.md
@.planning/phases/01-project-scaffold-and-core-ir/01-VALIDATION.md
@.planning/phases/01-project-scaffold-and-core-ir/01-PATTERNS.md
@AGENTS.md
@package.json
@tsconfig.json

<interfaces>
This plan creates the following public contracts:

```typescript
export type DiagramDirection = "TB" | "LR" | "BT" | "RL";
export interface Point { x: number; y: number; }
export interface Size { width: number; height: number; }
export interface Box extends Point, Size {}
export interface IntentDiagram { id?: string; title?: string; direction?: DiagramDirection; nodes: IntentNode[]; edges?: IntentEdge[]; groups?: IntentGroup[]; constraints?: Constraint[]; metadata?: JsonObject; }
export interface NormalizedDiagram { id: string; title?: string; direction: DiagramDirection; nodes: NormalizedNode[]; edges: NormalizedEdge[]; groups: NormalizedGroup[]; constraints: Constraint[]; diagnostics: Diagnostic[]; metadata?: JsonObject; }
export interface CoordinatedDiagram { id: string; title?: string; direction: DiagramDirection; nodes: CoordinatedNode[]; edges: CoordinatedEdge[]; groups: CoordinatedGroup[]; diagnostics: Diagnostic[]; bounds: Box; metadata?: JsonObject; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add public API type import test</name>
  <files>test/public-api.test.ts</files>
  <read_first>
    - .planning/phases/01-project-scaffold-and-core-ir/01-CONTEXT.md
    - .planning/phases/01-project-scaffold-and-core-ir/01-RESEARCH.md
    - .planning/phases/01-project-scaffold-and-core-ir/01-PATTERNS.md
    - src/index.ts if it already exists
  </read_first>
  <behavior>
    - Test 1: A test file can import `type IntentDiagram`, `type NormalizedDiagram`, `type CoordinatedDiagram`, `type Point`, `type Box`, `type IntentNode`, `type IntentEdge`, `type Label`, `type Constraint`, and `type Diagnostic` from `../src/index.js`.
    - Test 2: A sample `CoordinatedDiagram` object type-checks with `bounds`, `nodes`, `edges`, `groups`, and `diagnostics`.
  </behavior>
  <action>Create `test/public-api.test.ts` first. Import `describe`, `expect`, and `it` from "vitest". Import required public types from `../src/index.js` using `import type`. Add a runtime assertion `expect(sample.bounds.width).toBe(120)` against a `const sample: CoordinatedDiagram`. The test is allowed to fail until Tasks 2 and 3 create exports. Do not import from `../src/ir/...` in this test; FND-02 is about the public entrypoint.</action>
  <verify>
    <automated>rtk rg 'import type \\{[^}]*IntentDiagram[^}]*CoordinatedDiagram[^}]*Constraint' test/public-api.test.ts</automated>
    <automated>rtk rg 'from "../src/index\\.js"' test/public-api.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test/public-api.test.ts` contains `import type {`.
    - `test/public-api.test.ts` contains `IntentDiagram`.
    - `test/public-api.test.ts` contains `NormalizedDiagram`.
    - `test/public-api.test.ts` contains `CoordinatedDiagram`.
    - `test/public-api.test.ts` imports from `../src/index.js`.
    - `test/public-api.test.ts` does not import from `../src/ir/`.
  </acceptance_criteria>
  <done>Public API test expresses the expected import surface before implementation is completed.</done>
</task>

<task type="auto">
  <name>Task 2: Create geometry, diagnostics, constraints, and element type modules</name>
  <files>src/ir/geometry.ts, src/ir/diagnostics.ts, src/ir/constraints.ts, src/ir/elements.ts</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-project-scaffold-and-core-ir/01-CONTEXT.md
    - .planning/phases/01-project-scaffold-and-core-ir/01-RESEARCH.md
    - .planning/phases/01-project-scaffold-and-core-ir/01-PATTERNS.md
  </read_first>
  <action>Create `src/ir/geometry.ts` exporting `JsonPrimitive`, `JsonValue`, `JsonObject`, `DiagramDirection`, `Point`, `Size`, `Box`, `Insets`, `AnchorName`, and `AnchorPoint`. Create `src/ir/diagnostics.ts` exporting `DiagnosticSeverity = "info" | "warning" | "error"`, `DiagnosticPathSegment = string | number`, and `Diagnostic { severity; code; message; path?: DiagnosticPathSegment[]; detail?: JsonObject }`. Create `src/ir/constraints.ts` exporting discriminated interfaces `ExactPositionConstraint`, `RelativePositionConstraint`, `AlignConstraint`, `DistributeConstraint`, `ContainmentConstraint`, and union `Constraint`; use concrete `kind` values "exact-position", "relative-position", "align", "distribute", and "containment"; include ids and node/group references but no algorithm implementation. Create `src/ir/elements.ts` exporting `NodeShape = "rectangle" | "rounded-rectangle" | "ellipse" | "diamond" | "parallelogram" | "hexagon" | "cylinder"`, `Label`, `NodeBase`, `IntentNode`, `NormalizedNode`, `CoordinatedNode`, `EdgeEndpoint`, `IntentEdge`, `NormalizedEdge`, `CoordinatedEdge`, `IntentGroup`, `NormalizedGroup`, and `CoordinatedGroup`. Keep clear IR/type module boundaries per D-06 and future concern names per D-07. Keep the modules type-only and renderer-free per D-09, D-10, and D-11. Do not implement text measurement, shape geometry, layout, routing, exporters, CLI behavior, SVG, Excalidraw, draw.io, Mermaid, HTML, or CSS fields per D-08.</action>
  <verify>
    <automated>rtk rg 'export interface (Point|Size|Box|Insets)|export type DiagramDirection' src/ir/geometry.ts</automated>
    <automated>rtk rg 'export type Constraint|kind: \"exact-position\"|kind: \"containment\"' src/ir/constraints.ts</automated>
    <automated>rtk rg 'export interface (IntentNode|NormalizedNode|CoordinatedNode|IntentEdge|NormalizedEdge|CoordinatedEdge|Label)' src/ir/elements.ts</automated>
    <automated>rtk bash -lc "! rg -i 'svg|excalidraw|draw\\.io|drawio|mermaid|html|css' src/ir"</automated>
  </verify>
  <acceptance_criteria>
    - `src/ir/geometry.ts` contains `export interface Point`.
    - `src/ir/geometry.ts` contains `export interface Box`.
    - `src/ir/elements.ts` contains `export interface Label`.
    - `src/ir/elements.ts` contains `export interface IntentNode`.
    - `src/ir/elements.ts` contains `export interface CoordinatedEdge`.
    - `src/ir/constraints.ts` contains `export type Constraint =`.
    - `src/ir/diagnostics.ts` contains `export interface Diagnostic`.
    - `src/ir` files do not contain the strings `SVG`, `Excalidraw`, `draw.io`, `Mermaid`, `HTML`, or `CSS`.
  </acceptance_criteria>
  <done>Core IR building-block modules exist and cover all FND-02 named type categories without renderer-native fields.</done>
</task>

<task type="auto">
  <name>Task 3: Create diagram contracts and public barrels</name>
  <files>src/ir/diagram.ts, src/ir/index.ts, src/index.ts</files>
  <read_first>
    - src/ir/geometry.ts
    - src/ir/elements.ts
    - src/ir/constraints.ts
    - src/ir/diagnostics.ts
    - test/public-api.test.ts
    - .planning/phases/01-project-scaffold-and-core-ir/01-CONTEXT.md
  </read_first>
  <action>Create `src/ir/diagram.ts` exporting `IntentDiagram`, `NormalizedDiagram`, `CoordinatedDiagram`, `DiagramStage = "intent" | "normalized" | "coordinated"`, and `DiagramMetadata = JsonObject`. Add concise comments that `IntentDiagram` is authoring intent for future prepare, `NormalizedDiagram` is prepare output for future solve, and `CoordinatedDiagram` is solve output for future exporters per D-11. Create `src/ir/index.ts` re-exporting from `./geometry.js`, `./diagnostics.js`, `./constraints.js`, `./elements.js`, and `./diagram.js` to preserve the D-06 module boundary. Create `src/index.ts` with exactly `export * from "./ir/index.js";` for now; Plan 03 will add serialization exports. Run `rtk npm run typecheck && rtk npm test -- public-api` after implementation. Do not add broad package subpath exports in package.json.</action>
  <verify>
    <automated>rtk npm run typecheck</automated>
    <automated>rtk npm test -- public-api</automated>
  </verify>
  <acceptance_criteria>
    - `src/ir/diagram.ts` contains `export interface IntentDiagram`.
    - `src/ir/diagram.ts` contains `export interface NormalizedDiagram`.
    - `src/ir/diagram.ts` contains `export interface CoordinatedDiagram`.
    - `src/ir/diagram.ts` contains `future prepare`.
    - `src/ir/diagram.ts` contains `future solve`.
    - `src/ir/diagram.ts` contains `future exporters`.
    - `src/ir/index.ts` contains `export * from "./geometry.js";`.
    - `src/index.ts` contains `export * from "./ir/index.js";`.
    - `rtk npm run typecheck` exits 0.
    - `rtk npm test -- public-api` exits 0.
  </acceptance_criteria>
  <done>Public IR types are importable through the package entrypoint and verified by typecheck plus public API test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package consumer -> public TypeScript API | Downstream code imports DGE types and relies on names and semantics as the stable contract. |
| future exporters -> coordinated IR | Later exporters consume coordinated IR and must not require renderer-native fields in core types. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | public IR contracts | mitigate | Keep renderer-native fields out of `src/ir/*`; acceptance criteria grep for SVG/Excalidraw/draw.io/Mermaid/HTML/CSS strings. |
| T-01-05 | Information Disclosure | package entrypoint | mitigate | Export only through `src/index.ts` and package `"."`; tests import from `../src/index.js`, not internal module paths. |
| T-01-06 | Elevation of Privilege | type metadata | accept | Type-only interfaces have no runtime authority or execution path in Phase 1; runtime validation is explicitly deferred to DSL phase. |
</threat_model>

<verification>
Plan-level verification:

```bash
rtk npm run typecheck
rtk npm test -- public-api
rtk bash -lc "! rg -i 'svg|excalidraw|draw\\.io|drawio|mermaid|html|css' src/ir"
```
</verification>

<success_criteria>
- FND-02 named public type categories exist and are exported.
- Prepare/solve/export architecture appears in type comments.
- Public API test imports from `../src/index.js`.
- No renderer, CLI, layout, routing, DSL parser, text measurement, Pretext, or Dagre behavior is implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffold-and-core-ir/01-02-SUMMARY.md`.
</output>
