---
phase: 01-project-scaffold-and-core-ir
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/index.ts
  - src/serialization/canonical.ts
  - src/serialization/index.ts
  - test/serialization.test.ts
autonomous: true
requirements:
  - FND-03
must_haves:
  truths:
    - "Equivalent normalized/coordinated-like objects serialize to byte-identical canonical JSON."
    - "Canonical output sorts object keys and recognized arrays deterministically."
    - "Canonical output rounds finite numbers to 3 decimal places, normalizes -0 to 0, omits undefined, and rejects non-finite numbers."
  artifacts:
    - path: "src/serialization/canonical.ts"
      provides: "canonicalize and stringifyCanonical implementation"
      exports: ["DEFAULT_CANONICAL_PRECISION", "canonicalize", "stringifyCanonical"]
      contains: "DEFAULT_CANONICAL_PRECISION = 3"
    - path: "src/serialization/index.ts"
      provides: "serialization barrel"
    - path: "src/index.ts"
      provides: "public serialization exports"
      contains: 'from "./serialization/index.js"'
    - path: "test/serialization.test.ts"
      provides: "determinism, rounding, undefined omission, invalid-number tests"
  key_links:
    - from: "test/serialization.test.ts"
      to: "src/index.ts"
      via: "public serializer import"
      pattern: 'from "../src/index\\.js"'
    - from: "src/index.ts"
      to: "src/serialization/index.ts"
      via: "public re-export"
      pattern: 'from "./serialization/index\\.js"'
---

<objective>
Implement and verify DGE's Phase 1 canonical serializer.

Purpose: This plan satisfies FND-03 by making normalized/coordinated output byte-stable for tests, snapshots, and future exporter fixtures.
Output: Canonicalization utility, public serialization exports, and Vitest coverage for deterministic ordering and numeric integrity.
</objective>

<execution_context>
@/home/zhangyangrui/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-project-scaffold-and-core-ir/01-CONTEXT.md
@.planning/phases/01-project-scaffold-and-core-ir/01-RESEARCH.md
@.planning/phases/01-project-scaffold-and-core-ir/01-VALIDATION.md
@.planning/phases/01-project-scaffold-and-core-ir/01-PATTERNS.md
@AGENTS.md
@src/index.ts
@src/ir/index.ts
@test/public-api.test.ts

<interfaces>
Serializer public contract to create:

```typescript
export const DEFAULT_CANONICAL_PRECISION = 3;
export interface CanonicalizeOptions { precision?: number; }
export type CanonicalJson = null | boolean | number | string | CanonicalJson[] | { [key: string]: CanonicalJson };
export function canonicalize(value: unknown, options?: CanonicalizeOptions): CanonicalJson;
export function stringifyCanonical(value: unknown, precision?: number): string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add serializer determinism tests</name>
  <files>test/serialization.test.ts</files>
  <read_first>
    - .planning/phases/01-project-scaffold-and-core-ir/01-CONTEXT.md
    - .planning/phases/01-project-scaffold-and-core-ir/01-RESEARCH.md
    - .planning/phases/01-project-scaffold-and-core-ir/01-PATTERNS.md
    - src/index.ts
  </read_first>
  <behavior>
    - Test 1: `stringifyCanonical(a)` equals `stringifyCanonical(b)` for objects with different object key insertion order and node array order.
    - Test 2: numbers round to 3 decimals by default, so `2.1239` appears as `2.124`.
    - Test 3: `-0` serializes as `0`.
    - Test 4: properties with `undefined` are omitted from objects.
    - Test 5: `NaN`, `Infinity`, and `-Infinity` throw errors instead of serializing to JSON `null`.
  </behavior>
  <action>Create `test/serialization.test.ts` before implementation per D-14. Import `describe`, `expect`, and `it` from "vitest". Import `DEFAULT_CANONICAL_PRECISION`, `canonicalize`, and `stringifyCanonical` from `../src/index.js`. Build fixtures using `nodes`, `edges`, `groups`, `constraints`, `diagnostics`, `anchors`, and `points` arrays. Assert equivalent objects serialize equally, object keys in output appear sorted, arrays with object ids sort by `id`, edge-like arrays sort by `id` when present and then `sourceId`/`targetId`, finite numbers round to 3 decimals per D-13, `-0` is output as `0`, undefined object values are absent, and non-finite numbers throw `/Non-finite number/`. The tests may fail until Task 2 and Task 3 implement exports.</action>
  <verify>
    <automated>rtk rg 'stringifyCanonical|DEFAULT_CANONICAL_PRECISION|Non-finite number|2\\.124' test/serialization.test.ts</automated>
    <automated>rtk rg 'from "../src/index\\.js"' test/serialization.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test/serialization.test.ts` contains `stringifyCanonical`.
    - `test/serialization.test.ts` contains `DEFAULT_CANONICAL_PRECISION`.
    - `test/serialization.test.ts` contains `2.124`.
    - `test/serialization.test.ts` contains `Non-finite number`.
    - `test/serialization.test.ts` imports from `../src/index.js`.
    - `test/serialization.test.ts` includes assertions for undefined omission and `-0`.
  </acceptance_criteria>
  <done>Serializer test file captures FND-03 determinism and numeric-integrity requirements before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement canonical serializer</name>
  <files>src/serialization/canonical.ts, src/serialization/index.ts</files>
  <read_first>
    - test/serialization.test.ts
    - .planning/phases/01-project-scaffold-and-core-ir/01-CONTEXT.md
    - .planning/phases/01-project-scaffold-and-core-ir/01-RESEARCH.md
    - .planning/phases/01-project-scaffold-and-core-ir/01-PATTERNS.md
  </read_first>
  <action>Create `src/serialization/canonical.ts` exporting `DEFAULT_CANONICAL_PRECISION = 3`, `CanonicalizeOptions`, `CanonicalJson`, `canonicalize(value, options = {})`, and `stringifyCanonical(value, precision = DEFAULT_CANONICAL_PRECISION)` per D-12 and D-13. Implement recursive canonicalization for JSON-like values: return null/boolean/string as-is; for numbers, throw `new TypeError("Non-finite number cannot be canonicalized")` when `Number.isFinite(value)` is false, normalize `Object.is(value, -0)` to 0, and round finite numbers with `Math.round(value * 10 ** precision) / 10 ** precision`; for arrays, canonicalize each element and stable-sort arrays of plain objects using deterministic keys in this order: `id`, then `sourceId`, then `targetId`, then `nodeId`, then `groupId`, then `kind`, then JSON string of the canonical object; for plain objects, use `Object.keys(value).sort()`, omit keys where the raw value is `undefined`, and use only own enumerable properties so prototypes are ignored. Reject functions, symbols, and bigint with `TypeError("Unsupported value cannot be canonicalized")`. Create `src/serialization/index.ts` exporting from `./canonical.js`. Do not mutate the input object.</action>
  <verify>
    <automated>rtk npm test -- serialization</automated>
  </verify>
  <acceptance_criteria>
    - `src/serialization/canonical.ts` contains `export const DEFAULT_CANONICAL_PRECISION = 3`.
    - `src/serialization/canonical.ts` contains `Number.isFinite`.
    - `src/serialization/canonical.ts` contains `Object.is(value, -0)`.
    - `src/serialization/canonical.ts` contains `Object.keys(value).sort()`.
    - `src/serialization/canonical.ts` contains `rawValue === undefined`.
    - `src/serialization/canonical.ts` contains `Unsupported value cannot be canonicalized`.
    - `src/serialization/index.ts` contains `export * from "./canonical.js";`.
    - `rtk npm test -- serialization` exits 0.
  </acceptance_criteria>
  <done>Canonical serializer implementation passes determinism and numeric-integrity tests.</done>
</task>

<task type="auto">
  <name>Task 3: Export serializer publicly and run phase gate</name>
  <files>src/index.ts</files>
  <read_first>
    - src/index.ts
    - src/serialization/index.ts
    - test/public-api.test.ts
    - test/serialization.test.ts
    - package.json
  </read_first>
  <action>Update `src/index.ts` so it exports both IR and serialization APIs: keep `export * from "./ir/index.js";` and add `export * from "./serialization/index.js";`. Run the full Phase 1 gate with `rtk npm run verify`. If Biome reports formatting-only issues in files created by Phase 1, run `rtk npm run format` once and then rerun `rtk npm run verify`. Do not add any Phase 2+ behavior or dependencies while fixing verification.</action>
  <verify>
    <automated>rtk npm run verify</automated>
  </verify>
  <acceptance_criteria>
    - `src/index.ts` contains `export * from "./ir/index.js";`.
    - `src/index.ts` contains `export * from "./serialization/index.js";`.
    - `rtk npm run verify` exits 0.
    - `rtk rg '@chenglou/pretext|@dagrejs/dagre|commander|react|excalidraw|mermaid|svg|drawio' package.json src test` returns no matches, except this acceptance criterion text is not in repo files.
  </acceptance_criteria>
  <done>Serializer is public through the package entrypoint and the complete Phase 1 build/typecheck/test/lint gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package consumer -> canonicalize/stringifyCanonical | Callers may pass unknown objects into public serializer utilities. |
| geometry math later -> canonical JSON | Future numeric output must not silently serialize invalid geometry values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | canonicalize object traversal | mitigate | Use `Object.keys(value).sort()` and own enumerable properties only; do not follow prototypes or inherited properties. |
| T-01-08 | Tampering | numeric serialization | mitigate | Reject `NaN`, `Infinity`, and `-Infinity`; normalize `-0`; round finite numbers to `DEFAULT_CANONICAL_PRECISION = 3`. |
| T-01-09 | Denial of Service | recursive serializer | accept | Phase 1 serializer handles project-owned JSON-like IR, not arbitrary untrusted DSL input; runtime DSL validation and limits are deferred to Phase 5. |
</threat_model>

<verification>
Plan-level verification:

```bash
rtk npm test -- serialization
rtk npm run verify
```
</verification>

<success_criteria>
- FND-03 canonical serializer exports exist.
- Equivalent objects with reordered keys/arrays serialize byte-identically.
- Non-finite numbers throw instead of becoming JSON `null`.
- Full Phase 1 gate `rtk npm run verify` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffold-and-core-ir/01-03-SUMMARY.md`.
</output>
