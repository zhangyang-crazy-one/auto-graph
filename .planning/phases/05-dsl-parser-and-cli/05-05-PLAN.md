---
phase: 05-dsl-parser-and-cli
plan: 05
type: execute
wave: 5
depends_on:
  - 05-04
files_modified:
  - examples/architecture.yaml
  - examples/flowchart.yaml
  - examples/edge-labels.yaml
  - examples/groups.yaml
  - examples/hybrid-layout.yaml
  - test/fixtures/phase-05/architecture.yaml
  - test/fixtures/phase-05/flowchart.yaml
  - test/fixtures/phase-05/edge-labels.yaml
  - test/fixtures/phase-05/groups.yaml
  - test/fixtures/phase-05/hybrid-layout.yaml
  - test/fixtures/phase-05/architecture.json
  - test/dsl.test.ts
  - test/cli.test.ts
autonomous: true
requirements:
  - DSL-01
  - DSL-03
  - CLI-01
  - CLI-02
must_haves:
  truths:
    - "Example DSL files cover architecture, flowchart, edge labels, groups, and hybrid layout."
    - "Examples primarily use YAML and are agent-friendly."
    - "At least one JSON fixture proves YAML and JSON normalize into equivalent internal output."
    - "Every example parses, solves, and exports through the public CLI path."
  artifacts:
    - path: "examples/architecture.yaml"
      provides: "user-facing architecture DSL example"
      contains: "layout:"
    - path: "examples/flowchart.yaml"
      provides: "user-facing flowchart DSL example"
      contains: "edges:"
    - path: "examples/edge-labels.yaml"
      provides: "edge label shorthand example"
      contains: "->"
    - path: "examples/groups.yaml"
      provides: "group containment example"
      contains: "groups:"
    - path: "examples/hybrid-layout.yaml"
      provides: "fixed plus automatic layout example"
      contains: "position:"
    - path: "test/fixtures/phase-05/architecture.json"
      provides: "JSON parity fixture"
      contains: "\"nodes\""
  key_links:
    - from: "test/cli.test.ts"
      to: "examples/*.yaml"
      via: "runCli --input smoke"
      pattern: "examples"
    - from: "test/dsl.test.ts"
      to: "test/fixtures/phase-05/architecture.json"
      via: "YAML/JSON equivalence assertion"
      pattern: "architecture\\.json"
---

<objective>
Add Phase 5 examples and fixture smoke tests.

Purpose: Wave 5 proves the DSL is usable for the roadmap's required diagram families and that examples are not stale prose.
Output: YAML-first examples, fixture copies, one JSON parity fixture, and parse/solve/export smoke tests.
</objective>

<execution_context>
@/home/zhangyangrui/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-dsl-parser-and-cli/05-CONTEXT.md
@.planning/phases/05-dsl-parser-and-cli/05-RESEARCH.md
@.planning/phases/05-dsl-parser-and-cli/05-VALIDATION.md
@.planning/phases/05-dsl-parser-and-cli/05-PATTERNS.md
@.planning/phases/05-dsl-parser-and-cli/05-04-SUMMARY.md
@test/fixtures/phase-04/coordinated-export.canonical.json
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add YAML-first DSL examples and fixture copies</name>
  <files>examples/architecture.yaml, examples/flowchart.yaml, examples/edge-labels.yaml, examples/groups.yaml, examples/hybrid-layout.yaml, test/fixtures/phase-05/architecture.yaml, test/fixtures/phase-05/flowchart.yaml, test/fixtures/phase-05/edge-labels.yaml, test/fixtures/phase-05/groups.yaml, test/fixtures/phase-05/hybrid-layout.yaml, test/fixtures/phase-05/architecture.json</files>
  <read_first>
    .planning/phases/05-dsl-parser-and-cli/05-CONTEXT.md
    .planning/phases/05-dsl-parser-and-cli/05-RESEARCH.md
    .planning/phases/05-dsl-parser-and-cli/05-VALIDATION.md
    test/fixtures/phase-03/hybrid-layout.canonical.json
    test/fixtures/phase-04/coordinated-export.canonical.json
  </read_first>
  <action>
    Follow `05-PATTERNS.md` fixture analogs from `test/fixtures/phase-03` and `test/fixtures/phase-04`: examples and fixtures are intentionally grouped in this single fixture-heavy plan so smoke tests can prove the complete required example family set together. Create repo-root `examples/` YAML files for exactly these five diagram families: `architecture.yaml`, `flowchart.yaml`, `edge-labels.yaml`, `groups.yaml`, and `hybrid-layout.yaml`. Keep examples concise and agent-friendly per D-01. Each example must include `title`, `layout.direction`, `nodes` object map per D-03, and `edges`. `edge-labels.yaml` must use both `api -> db: reads` shorthand and at least one structured edge with label. `groups.yaml` must use `groups.backend.nodes: [...]` per D-08. `hybrid-layout.yaml` must include at least one inline fixed `position` per D-06 and one structured relative-position constraint per D-07. Add identical fixture copies under `test/fixtures/phase-05/`. Add `test/fixtures/phase-05/architecture.json` that represents the same diagram as `architecture.yaml` to prove YAML/JSON parity. Do not introduce draw.io, Mermaid, ASCII, browser preview, or rich style API fields.
  </action>
  <verify>
    <automated>rtk test -f examples/architecture.yaml && rtk test -f test/fixtures/phase-05/architecture.json</automated>
    <automated>rtk rg -n 'api -> db: reads|groups:|position:|relative-position' examples test/fixtures/phase-05</automated>
  </verify>
  <acceptance_criteria>
    `rtk find examples test/fixtures/phase-05 -maxdepth 1 -type f | rtk sort` lists all five YAML examples and the JSON parity fixture.
    `rtk rg -n '^nodes:|^edges:|layout:|direction:' examples/*.yaml` finds required top-level sections in every YAML file.
    `rtk rg -n 'api -> db: reads' examples/edge-labels.yaml test/fixtures/phase-05/edge-labels.yaml` finds edge shorthand.
    `rtk rg -n 'groups:|backend:|nodes:' examples/groups.yaml test/fixtures/phase-05/groups.yaml` finds group containment.
    `rtk rg -n 'position:|relative-position' examples/hybrid-layout.yaml test/fixtures/phase-05/hybrid-layout.yaml` finds hybrid layout semantics.
  </acceptance_criteria>
  <done>Phase 5 example and fixture YAML/JSON files cover architecture, flowchart, edge labels, groups, and hybrid layout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add fixture smoke tests for parse, solve, and export</name>
  <files>test/dsl.test.ts, test/cli.test.ts</files>
  <read_first>
    src/dsl/render.ts
    src/dsl/normalize.ts
    src/cli/run.ts
    test/fixtures/phase-05/architecture.yaml
    test/fixtures/phase-05/architecture.json
    .planning/phases/05-dsl-parser-and-cli/05-VALIDATION.md
  </read_first>
  <action>
    Follow `05-PATTERNS.md` test analogs from `test/determinism.test.ts`, `test/exporters.test.ts`, and `test/solver.test.ts`: use table-driven fixture tests and canonical comparison where parity matters. Extend `test/dsl.test.ts` with a table over the five Phase 5 YAML fixtures that calls `renderDiagramDsl(source, { sourcePath })` and asserts exit-equivalent success: no error diagnostics, non-empty content, SVG output starts with `<svg`, and coordinated/render metadata includes the expected title when available. Add a YAML/JSON parity test for `architecture.yaml` and `architecture.json` that normalizes both and compares `nodes`, `edges`, `groups`, and `constraints` via `stringifyCanonical`. Extend `test/cli.test.ts` with fixture smoke tests that call `runCli(["--input", fixturePath, "--format", "svg"])` and `runCli(["--input", fixturePath, "--format", "excalidraw"])`, asserting stdout contains SVG or valid JSON and stderr has no error diagnostics. These tests implement Phase 5 success criterion 5 and requirements DSL-01, DSL-03, CLI-01, and CLI-02.
  </action>
  <verify>
    <automated>rtk npm exec vitest -- test/dsl.test.ts test/cli.test.ts</automated>
    <automated>rtk npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    `rtk rg -n 'architecture|flowchart|edge-labels|groups|hybrid-layout' test/dsl.test.ts test/cli.test.ts` finds all fixture names.
    `rtk rg -n 'stringifyCanonical|architecture\\.json' test/dsl.test.ts` finds YAML/JSON parity coverage.
    Tests prove all five YAML examples parse, solve, and export to SVG.
    Tests prove at least one fixture exports to Excalidraw JSON through CLI stdout.
  </acceptance_criteria>
  <done>Fixture smoke tests prove examples parse, solve, and export through DSL and CLI paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed examples -> public user input | Examples become copied input for agents and users. |
| fixture files -> CLI smoke tests | Fixture paths exercise CLI read and export paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service | example/fixture size | mitigate | Keep examples small and test through the same parser/CLI size guards. |
| T-05-02 | Tampering | example DSL object maps | mitigate | Fixture tests must pass through parser/schema/normalizer; examples cannot bypass validation. |
| T-05-03 | Tampering | CLI output in smoke tests | mitigate | Smoke tests use stdout for examples and rely on Wave 4 atomic write tests for file output. |
| T-05-04 | Tampering | fixture output formats | mitigate | Examples use only `svg` and `excalidraw`; deferred formats do not appear as supported outputs. |
| T-05-05 | Spoofing | example labels and diagnostics | mitigate | Examples avoid terminal-control characters and tests run through stable diagnostic formatting. |
</threat_model>

<verification>
Run `rtk npm exec vitest -- test/dsl.test.ts test/cli.test.ts` and `rtk npm run typecheck`.
</verification>

<success_criteria>
Wave 5 is complete when all five required DSL example families exist, YAML remains the primary authoring surface, JSON parity is tested, and examples parse/solve/export through the same public code path users run.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dsl-parser-and-cli/05-05-SUMMARY.md`.
</output>
