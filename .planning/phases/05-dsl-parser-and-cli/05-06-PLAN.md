---
phase: 05-dsl-parser-and-cli
plan: 06
type: execute
wave: 6
depends_on:
  - 05-05
files_modified:
  - src/index.ts
  - test/public-api.test.ts
  - test/exporters.test.ts
  - test/determinism.test.ts
  - README.md
autonomous: true
requirements:
  - DSL-01
  - DSL-02
  - DSL-03
  - CLI-01
  - CLI-02
  - CLI-03
must_haves:
  truths:
    - "Selected DSL APIs are importable from the package root."
    - "The built package includes a working dge binary."
    - "Static guards prove src/dsl and src/cli do not import geometry/layout/routing/export implementation internals beyond allowed pipeline APIs."
    - "Full project verification passes after Phase 5."
  artifacts:
    - path: "src/index.ts"
      provides: "root export of selected DSL API"
      contains: "./dsl/index.js"
    - path: "test/public-api.test.ts"
      provides: "root import proof for DSL APIs"
      contains: "parseDiagramDsl"
    - path: "test/exporters.test.ts"
      provides: "static anti-recompute guard extended to DSL/CLI"
      contains: "src/dsl"
    - path: "test/determinism.test.ts"
      provides: "Phase 5 deterministic DSL render proof"
      contains: "phase-05"
    - path: "README.md"
      provides: "minimal CLI usage documentation"
      contains: "dge --input"
  key_links:
    - from: "src/index.ts"
      to: "src/dsl/index.ts"
      via: "root barrel export"
      pattern: "./dsl/index\\.js"
    - from: "test/exporters.test.ts"
      to: "src/dsl and src/cli"
      via: "forbidden import regex"
      pattern: "src/dsl|src/cli"
    - from: "package.json"
      to: "dist/cli/index.js"
      via: "bin.dge build smoke"
      pattern: "\"dge\""
---

<objective>
Publish the selected DSL API and close Phase 5 with static guards and full verification.

Purpose: Wave 6 turns the implementation into a stable public surface, proves the CLI builds, and prevents future parser/CLI geometry recomputation drift.
Output: Root API export, public API tests, deterministic render tests, static anti-recompute guards, README CLI notes, and full verify.
</objective>

<execution_context>
@/home/zhangyangrui/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-dsl-parser-and-cli/05-CONTEXT.md
@.planning/phases/05-dsl-parser-and-cli/05-RESEARCH.md
@.planning/phases/05-dsl-parser-and-cli/05-VALIDATION.md
@.planning/phases/05-dsl-parser-and-cli/05-PATTERNS.md
@.planning/phases/04-coordinated-exporters/04-04-SUMMARY.md
@.planning/phases/05-dsl-parser-and-cli/05-05-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish selected DSL APIs and deterministic package gates</name>
  <files>src/index.ts, test/public-api.test.ts, test/determinism.test.ts, README.md</files>
  <read_first>
    src/dsl/index.ts
    src/index.ts
    test/public-api.test.ts
    test/determinism.test.ts
    test/fixtures/phase-05/architecture.yaml
    package.json
    README.md
    .planning/phases/05-dsl-parser-and-cli/05-CONTEXT.md
  </read_first>
  <action>
    Follow `05-PATTERNS.md` public API analogs from `src/index.ts` and `test/public-api.test.ts`: public DSL APIs must be root-exported and verified from the package entrypoint. Add `export * from "./dsl/index.js";` to `src/index.ts`. Publicly expose only selected stable APIs from `src/dsl/index.ts`: `parseDiagramDsl`, `normalizeDiagramDsl`, `renderDiagramDsl`, `resolveOutputFormat`, `parseEdgeShorthand`, and the exported DSL types. Keep low-level Zod schema objects private unless already necessary for tests. Extend `test/public-api.test.ts` to import these functions/types from `../src/index.js` and run a small valid YAML sample through parse/normalize/render. Extend `test/determinism.test.ts` with a Phase 5 fixture test that renders `test/fixtures/phase-05/architecture.yaml` twice and compares `stringifyCanonical` of normalized/coordinated outputs and raw SVG content for byte stability. Add a concise README section with exact commands `dge --input examples/architecture.yaml --format svg --output architecture.svg`, `cat examples/architecture.yaml | dge --format excalidraw`, and `cat bad.yaml | dge --json`, documenting default format `svg` and flag precedence CLI `--format` > DSL `output.format` > `svg` per D-05 and D-17.
  </action>
  <verify>
    <automated>rtk npm test -- public-api determinism</automated>
    <automated>rtk npm run build</automated>
  </verify>
  <acceptance_criteria>
    `rtk rg -n './dsl/index\\.js' src/index.ts` finds the root export.
    `rtk rg -n 'parseDiagramDsl|normalizeDiagramDsl|renderDiagramDsl|resolveOutputFormat|parseEdgeShorthand' test/public-api.test.ts src/dsl/index.ts` finds public API coverage.
    `rtk rg -n 'phase-05|architecture\\.yaml|stringifyCanonical' test/determinism.test.ts` finds deterministic DSL rendering coverage.
    `rtk rg -n 'dge --input examples/architecture\\.yaml --format svg --output architecture\\.svg|--json|output\\.format' README.md` finds CLI documentation.
    `rtk test -f dist/cli/index.js` exits 0 after build.
  </acceptance_criteria>
  <done>Selected DSL APIs are exported from the root entrypoint and package bin/build behavior is verified.</done>
</task>

<task type="auto">
  <name>Task 2: Add static anti-recompute guards and run full Phase 5 verification</name>
  <files>test/exporters.test.ts</files>
  <read_first>
    test/exporters.test.ts
    src/dsl/render.ts
    src/cli/run.ts
    src/solver/solve.ts
    src/exporters/index.ts
    .planning/phases/05-dsl-parser-and-cli/05-RESEARCH.md
    .planning/phases/04-coordinated-exporters/04-04-SUMMARY.md
  </read_first>
  <action>
    Follow `05-PATTERNS.md` static guard analog from `test/exporters.test.ts`: scan source files for forbidden recomputation imports/calls and fail with explicit messages. Extend the existing static recomputation guard pattern in `test/exporters.test.ts` or create an adjacent test section in the same file. Scan `src/dsl` and `src/cli` files and fail if they import or call forbidden implementation internals: `runDagreInitialLayout`, `applyLayoutConstraints`, `routeEdge`, `computeShapeGeometry`, `computeContainerGeometry`, `unionBoxes`, direct `../layout/`, direct `../routing/`, direct `../geometry/`, or direct text backend imports except allowed label fitting through `fitLabel` in `src/dsl/normalize.ts`. Allow these explicit pipeline APIs only: `solveDiagram`, `exportSvg`, `exportExcalidraw`, `fitLabel`, `DeterministicTextMeasurer`, parser packages `yaml` and `zod`, and CLI package `commander`. Add a package-bin smoke check that runs the built `dist/cli/index.js` against `examples/architecture.yaml` after `rtk npm run build` and asserts SVG output. Finish with `rtk npm run verify`. This enforces D-02, Phase 4 exporter boundary, and the Phase 5 static anti-recompute requirement.
  </action>
  <verify>
    <automated>rtk npm run verify</automated>
  </verify>
  <acceptance_criteria>
    `rtk rg -n 'src/dsl|src/cli|runDagreInitialLayout|applyLayoutConstraints|routeEdge|computeShapeGeometry|computeContainerGeometry|forbidden' test/exporters.test.ts` finds the static guard.
    `rtk rg -n 'dist/cli/index\\.js|examples/architecture\\.yaml' test/exporters.test.ts test/cli.test.ts` finds the built binary smoke coverage.
    `rtk npm run verify` passes.
    `rtk rg -n 'runDagreInitialLayout|applyLayoutConstraints|routeEdge|computeShapeGeometry|computeContainerGeometry|from "../layout|from "../routing|from "../geometry' src/dsl src/cli` prints no disallowed matches except test-approved allowed `fitLabel` usage in `src/dsl/normalize.ts`.
  </acceptance_criteria>
  <done>Static anti-recompute guards and full project verification close Phase 5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public API -> external callers | Root exports become package contract. |
| source modules -> architecture boundaries | Parser/CLI must not bypass solver/exporter ownership. |
| package build -> executable binary | Built CLI must match package bin metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service | public parser/CLI API | mitigate | Public tests exercise the same parser/CLI input size guards created in prior waves. |
| T-05-02 | Tampering | public normalization API | mitigate | Static guard and public tests ensure callers reach explicit normalization, not unsafe object spreads or geometry internals. |
| T-05-03 | Tampering | built binary output | mitigate | Full CLI tests from Wave 4 remain in `rtk npm run verify`, including atomic write preservation. |
| T-05-04 | Tampering | public output format API | mitigate | Root-exported `resolveOutputFormat` and tests preserve `svg`/`excalidraw` only and reject future formats. |
| T-05-05 | Spoofing | README and public diagnostics | mitigate | README documents `--json`; verify suite keeps stable machine-readable diagnostics and no ANSI/source-snippet injection in JSON. |
</threat_model>

<verification>
Run `rtk npm test -- public-api determinism`, `rtk npm run build`, and final `rtk npm run verify`.
</verification>

<success_criteria>
Phase 5 is complete when root DSL APIs are importable, the package builds a working `dge` binary, parser/CLI static guards prevent geometry recomputation, README shows exact CLI usage, and `rtk npm run verify` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dsl-parser-and-cli/05-06-SUMMARY.md`.
</output>
