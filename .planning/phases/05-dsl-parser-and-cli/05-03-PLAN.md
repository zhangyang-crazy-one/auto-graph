---
phase: 05-dsl-parser-and-cli
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - src/dsl/normalize.ts
  - src/dsl/render.ts
  - src/dsl/diagnostics.ts
  - src/dsl/index.ts
  - src/dsl/types.ts
  - test/dsl.test.ts
  - test/dsl-diagnostics.test.ts
autonomous: true
requirements:
  - DSL-01
  - DSL-02
  - DSL-03
must_haves:
  truths:
    - "Validated DSL normalizes into existing NormalizedDiagram without becoming a second semantic model."
    - "Node maps, group maps, fixed positions, layout defaults, routing defaults, and structured constraints reach the existing solver contracts."
    - "Missing node/group/constraint references produce validate-layer errors before export."
    - "Final render dispatch uses solveDiagram and the Phase 4 exporters only."
  artifacts:
    - path: "src/dsl/normalize.ts"
      provides: "DSL to NormalizedDiagram bridge with deterministic defaults and label fitting"
      exports: ["normalizeDiagramDsl"]
    - path: "src/dsl/render.ts"
      provides: "parse/normalize/solve/export orchestration for library and CLI reuse"
      exports: ["renderDiagramDsl", "resolveOutputFormat", "exportDiagram"]
    - path: "test/dsl.test.ts"
      provides: "normalization, solver bridge, and export dispatch coverage"
      contains: "normalizeDiagramDsl"
    - path: "test/dsl-diagnostics.test.ts"
      provides: "missing reference and unsupported format diagnostics"
      contains: "validate.reference.missing"
  key_links:
    - from: "src/dsl/normalize.ts"
      to: "src/labels/fit.ts"
      via: "fitLabel"
      pattern: "fitLabel"
    - from: "src/dsl/render.ts"
      to: "src/solver/solve.ts"
      via: "solveDiagram"
      pattern: "solveDiagram"
    - from: "src/dsl/render.ts"
      to: "src/exporters/index.ts"
      via: "exportSvg and exportExcalidraw"
      pattern: "exportSvg[\\s\\S]*exportExcalidraw"
---

<objective>
Normalize parsed DSL into solver-ready diagrams and dispatch through the existing solve/export pipeline.

Purpose: Wave 3 is the architecture boundary that proves Phase 5 is only an authoring/execution layer over existing IR, solver, and exporters.
Output: `normalizeDiagramDsl()`, `renderDiagramDsl()`, semantic diagnostics, output format resolution, and tests for DSL-01 through DSL-03.
</objective>

<execution_context>
@/home/zhangyangrui/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-dsl-parser-and-cli/05-CONTEXT.md
@.planning/phases/05-dsl-parser-and-cli/05-RESEARCH.md
@.planning/phases/05-dsl-parser-and-cli/05-VALIDATION.md
@.planning/phases/05-dsl-parser-and-cli/05-PATTERNS.md
@.planning/phases/04-coordinated-exporters/04-CONTEXT.md
@.planning/phases/04-coordinated-exporters/04-04-SUMMARY.md
@.planning/phases/05-dsl-parser-and-cli/05-02-SUMMARY.md
@AGENTS.md

<interfaces>
Existing solver input:
```typescript
export interface NormalizedDiagram {
  id: string;
  title?: string;
  direction: DiagramDirection;
  nodes: NormalizedNode[];
  edges: NormalizedEdge[];
  groups: NormalizedGroup[];
  constraints: Constraint[];
  diagnostics: Diagnostic[];
  metadata?: DiagramMetadata;
}
```

Existing exporter functions:
```typescript
export type ExportFormat = "svg" | "excalidraw";
export function exportSvg(diagram: CoordinatedDiagram, options?: ExportOptions): string;
export function exportExcalidraw(diagram: CoordinatedDiagram, options?: ExportOptions): string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize DSL maps and constraints into NormalizedDiagram</name>
  <files>src/dsl/normalize.ts, src/dsl/diagnostics.ts, src/dsl/index.ts, src/dsl/types.ts, test/dsl.test.ts, test/dsl-diagnostics.test.ts</files>
  <read_first>
    src/dsl/parse.ts
    src/dsl/schema.ts
    src/dsl/edges.ts
    src/ir/diagram.ts
    src/ir/elements.ts
    src/ir/constraints.ts
    src/labels/fit.ts
    src/text/fallback.ts
    src/text/pretext.ts
    test/solver.test.ts
    .planning/phases/05-dsl-parser-and-cli/05-CONTEXT.md
    .planning/phases/05-dsl-parser-and-cli/05-PATTERNS.md
  </read_first>
  <action>
    Follow `05-PATTERNS.md` normalization analogs from `test/solver.test.ts` and `src/solver/solve.ts`: build stable arrays, deterministic defaults, and solver-ready `NormalizedDiagram` values. Implement `normalizeDiagramDsl(dslValue, options?: { id?: string; textMeasurer?: TextMeasurer }): NormalizeDiagramDslResult` that returns `{ diagram?: NormalizedDiagram; diagnostics: DslDiagnostic[]; output?: { format?: DslOutputFormat } }`. Construct every normalized object explicitly, never by spreading untrusted DSL objects, per T-05-02. Defaults: diagram id `dslValue.id ?? "diagram"`, direction `dslValue.layout?.direction ?? dslValue.direction ?? "TB"`, node shape `"rectangle"`, node padding `{ top: 12, right: 16, bottom: 12, left: 16 }`, node min size `{ width: 80, height: 40 }`, group padding `{ top: 16, right: 16, bottom: 16, left: 16 }`, routing kind `"orthogonal"`. Fit labels with existing `fitLabel()` and the provided `textMeasurer` or `DeterministicTextMeasurer` fallback; use label `maxWidth` when provided, otherwise `160`. Convert node map keys to stable sorted `NormalizedNode[]`, edge shorthand/structured edges to stable `NormalizedEdge[]` with ids `${sourceId}-${targetId}` plus numeric suffix for duplicates, group map keys to `NormalizedGroup[]`, and structured constraints to existing `Constraint[]`. Implement D-02, D-03, D-06, D-07, D-08, and D-09 exactly.
  </action>
  <verify>
    <automated>rtk npm exec vitest -- test/dsl.test.ts test/dsl-diagnostics.test.ts</automated>
    <automated>rtk npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    `rtk rg -n 'normalizeDiagramDsl|NormalizedDiagram|fitLabel|DeterministicTextMeasurer' src/dsl/normalize.ts` finds the normalization bridge.
    `rtk rg -n 'top: 12|right: 16|width: 80|height: 40|maxWidth.*160|orthogonal' src/dsl/normalize.ts` finds deterministic defaults.
    Tests prove node object maps sort by id and become `NormalizedNode[]`.
    Tests prove `nodes.api.position: { x: 100, y: 80 }` is preserved on the normalized node.
    Tests prove relative, align, distribute, and containment constraints map to existing `Constraint.kind` values.
    Tests prove `groups.backend.nodes: [api, db]` becomes `NormalizedGroup.nodeIds`.
  </acceptance_criteria>
  <done>DSL normalization produces solver-ready diagrams with semantic diagnostics and deterministic defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add semantic validation, solve bridge, and export dispatch</name>
  <files>src/dsl/render.ts, src/dsl/normalize.ts, src/dsl/diagnostics.ts, src/dsl/index.ts, src/dsl/types.ts, test/dsl.test.ts, test/dsl-diagnostics.test.ts</files>
  <read_first>
    src/dsl/normalize.ts
    src/solver/solve.ts
    src/exporters/index.ts
    src/exporters/types.ts
    src/serialization/canonical.ts
    .planning/phases/04-coordinated-exporters/04-04-SUMMARY.md
    .planning/phases/05-dsl-parser-and-cli/05-RESEARCH.md
  </read_first>
  <action>
    Follow `05-PATTERNS.md` pipeline/export analogs from `src/solver/solve.ts`, `src/exporters/index.ts`, and `src/exporters/types.ts`: render must call existing `solveDiagram()`, `exportSvg()`, and `exportExcalidraw()` rather than duplicating geometry or serialization. Add semantic reference validation before returning a normalized diagram: missing edge source/target, group node/group ids, exact-position targets, relative source/reference ids, align/distribute target ids, and containment container/child ids must produce `validate` error diagnostics with code `validate.reference.missing`, exact DSL paths such as `["edges", 0, "source"]`, and repair hints. Error diagnostics block render output per D-13. Implement `resolveOutputFormat(cliFormat?: string, dslFormat?: DslOutputFormat): { format?: DslOutputFormat; diagnostics: DslDiagnostic[] }` with precedence CLI `--format` > DSL `output.format` > default `"svg"` per D-05 and D-17; unsupported formats return code `validate.output-format.unsupported`. Implement `exportDiagram(format: DslOutputFormat, diagram: CoordinatedDiagram): ExportResult` using only `exportSvg()` and `exportExcalidraw()`. Implement `renderDiagramDsl(source, options?: { sourcePath?: string; sourceFormat?: "yaml" | "json"; format?: string; textMeasurer?: TextMeasurer })` as parse -> normalize -> `solveDiagram()` -> diagnostic gate -> export; convert solver diagnostics to `solve` layer and exporter errors to `export` layer. Do not import Dagre, routing, geometry, or exporter internals in `src/dsl` except the existing public pipeline APIs required here.
  </action>
  <verify>
    <automated>rtk npm exec vitest -- test/dsl.test.ts test/dsl-diagnostics.test.ts</automated>
    <automated>rtk npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    `rtk rg -n 'renderDiagramDsl|resolveOutputFormat|exportDiagram|solveDiagram|exportSvg|exportExcalidraw' src/dsl/render.ts` finds the pipeline.
    `rtk rg -n 'validate\\.reference\\.missing|validate\\.output-format\\.unsupported' src/dsl test/dsl-diagnostics.test.ts` finds semantic diagnostics and tests.
    Tests prove CLI format overrides DSL output format.
    Tests prove default output format is `svg` when neither CLI nor DSL specifies a format.
    Tests prove `drawio`, `mermaid`, and `ascii` are rejected and do not silently succeed.
    Tests prove a valid DSL renders non-empty SVG and Excalidraw JSON through existing exporters.
  </acceptance_criteria>
  <done>DSL rendering dispatches through solveDiagram and existing exporters with format precedence tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validated DSL -> normalized IR | Untrusted authoring objects become solver input. |
| normalized IR -> solver/exporters | Existing trusted pipeline receives DSL-derived data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service | label fitting and normalization | mitigate | Reuse Wave 2 input byte limit; do not add recursive expansion or unbounded generation during normalization. |
| T-05-02 | Tampering | `normalizeDiagramDsl` | mitigate | Explicitly construct NormalizedDiagram objects and semantic checks; never use parsed object spread into IR. |
| T-05-03 | Tampering | output path | accept | This wave returns content only; Wave 4 owns atomic filesystem writes. |
| T-05-04 | Tampering | `resolveOutputFormat` | mitigate | Validate format once with `svg` and `excalidraw` only; unsupported formats block before export. |
| T-05-05 | Spoofing | solve/export diagnostics | mitigate | Convert diagnostics into stable layer-tagged DslDiagnostic values with sorted paths and no arbitrary source echo. |
</threat_model>

<verification>
Run `rtk npm exec vitest -- test/dsl.test.ts test/dsl-diagnostics.test.ts` and `rtk npm run typecheck`.
</verification>

<success_criteria>
Wave 3 is complete when valid DSL solves and exports through existing APIs, invalid references block with actionable diagnostics, output format precedence is tested, and DSL modules do not own geometry/routing/export internals.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dsl-parser-and-cli/05-03-SUMMARY.md`.
</output>
