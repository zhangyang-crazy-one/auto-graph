---
phase: 02-text-labels-and-shape-geometry
plan: 05
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
files_modified:
  - src/index.ts
  - test/public-api.test.ts
  - test/fixtures/phase-02/labels.canonical.json
  - test/fixtures/phase-02/shapes.canonical.json
  - test/fixtures/phase-02/containers.canonical.json
autonomous: true
requirements:
  - TXT-01
  - TXT-02
  - TXT-03
  - GEO-01
  - GEO-02
  - GEO-03
requirements_addressed:
  - TXT-01
  - TXT-02
  - TXT-03
  - GEO-01
  - GEO-02
  - GEO-03
must_haves:
  truths:
    - "All Phase 2 text, label, geometry, and container APIs are importable from the package root entrypoint."
    - "Canonical numeric fixtures lock labels, shapes, and containers without SVG generation."
    - "Renderer creep checks and the full verify command pass for Phase 2."
  artifacts:
    - path: "src/index.ts"
      provides: "Root public exports for text, labels, and geometry barrels"
    - path: "test/public-api.test.ts"
      provides: "Public import proof for Phase 2 APIs"
    - path: "test/fixtures/phase-02/labels.canonical.json"
      provides: "Stable label layout fixture"
    - path: "test/fixtures/phase-02/shapes.canonical.json"
      provides: "Stable shape geometry fixture"
    - path: "test/fixtures/phase-02/containers.canonical.json"
      provides: "Stable container geometry fixture"
  key_links:
    - from: "src/index.ts"
      to: "src/text/index.ts"
      via: "root barrel export"
      pattern: "export \\* from \"\\./text/index\\.js\""
    - from: "src/index.ts"
      to: "src/labels/index.ts"
      via: "root barrel export"
      pattern: "export \\* from \"\\./labels/index\\.js\""
    - from: "src/index.ts"
      to: "src/geometry/index.ts"
      via: "root barrel export"
      pattern: "export \\* from \"\\./geometry/index\\.js\""
    - from: "test/public-api.test.ts"
      to: "../src/index.js"
      via: "public entrypoint import"
      pattern: "from \"../src/index\\.js\""
---

<objective>
Wire Phase 2 into the public API and lock full-phase numeric fixtures.

Purpose: Finish Phase 2 by exposing all new modules through the root package entrypoint, adding canonical fixtures for deterministic geometry contracts, checking renderer-neutral boundaries, and running the full verification gate.
Output: Public API updates, fixture files, renderer-creep checks, and full verify.
</objective>

<execution_context>
@/home/zhangyangrui/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-text-labels-and-shape-geometry/02-CONTEXT.md
@.planning/phases/02-text-labels-and-shape-geometry/02-RESEARCH.md
@.planning/phases/02-text-labels-and-shape-geometry/02-VALIDATION.md
@.planning/phases/02-text-labels-and-shape-geometry/02-PATTERNS.md
@.planning/phases/02-text-labels-and-shape-geometry/02-01-SUMMARY.md
@.planning/phases/02-text-labels-and-shape-geometry/02-02-SUMMARY.md
@.planning/phases/02-text-labels-and-shape-geometry/02-03-SUMMARY.md
@.planning/phases/02-text-labels-and-shape-geometry/02-04-SUMMARY.md
@src/index.ts
@src/text/index.ts
@src/labels/index.ts
@src/geometry/index.ts
@src/serialization/canonical.ts
@test/public-api.test.ts

<interfaces>
Root public export pattern from Phase 1:
```typescript
export * from "./ir/index.js";
export * from "./serialization/index.js";
```

Required final root exports:
```typescript
export * from "./geometry/index.js";
export * from "./ir/index.js";
export * from "./labels/index.js";
export * from "./serialization/index.js";
export * from "./text/index.js";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend public API import proof for Phase 2</name>
  <files>src/index.ts, test/public-api.test.ts</files>
  <read_first>
    - `src/index.ts` and `test/public-api.test.ts`.
    - `.planning/phases/01-project-scaffold-and-core-ir/01-VERIFICATION.md` public API verification.
    - `.planning/phases/02-text-labels-and-shape-geometry/02-PATTERNS.md` `src/index.ts` and public API guidance.
    - `src/text/index.ts`, `src/labels/index.ts`, and `src/geometry/index.ts` from prior plans.
  </read_first>
  <behavior>
    - RED then green test: `test/public-api.test.ts` can import representative Phase 2 types/classes/functions from `../src/index.js`.
    - The test constructs a deterministic label layout, shape geometry, and container geometry through root imports.
    - The package `exports` field remains root-only; no `./text`, `./labels`, or `./geometry` subpath exports are added.
  </behavior>
  <action>
    Update `src/index.ts` to re-export `./geometry/index.js`, `./ir/index.js`, `./labels/index.js`, `./serialization/index.js`, and `./text/index.js`. Preserve existing IR and serialization exports. Update `test/public-api.test.ts` with a new test importing `DeterministicTextMeasurer`, `fitLabel`, `computeShapeGeometry`, `computeContainerGeometry`, `expandBox`, and representative Phase 2 types from `../src/index.js`. Add a Node-side assertion in the test or a separate check that `package.json.exports` still contains only `"."`.
  </action>
  <acceptance_criteria>
    - `src/index.ts` contains root exports for `geometry`, `ir`, `labels`, `serialization`, and `text`, all with `.js` extension paths.
    - `test/public-api.test.ts` imports Phase 2 APIs from `../src/index.js`.
    - `test/public-api.test.ts` includes a root-only package exports assertion or equivalent test.
    - `package.json` does not contain `"./text"`, `"./labels"`, `"./geometry"`, or other Phase 2 subpath exports.
    - `rtk npm test -- public-api` passes.
    - `rtk npm run typecheck` passes.
  </acceptance_criteria>
  <verify>
    <automated>rtk npm test -- public-api</automated>
    <automated>rtk npm run typecheck</automated>
    <automated>rtk node -e "const p=require('./package.json'); const keys=Object.keys(p.exports); if (keys.length !== 1 || keys[0] !== '.') process.exit(1);"</automated>
  </verify>
  <done>All Phase 2 APIs are publicly importable through the package root while package exports remain root-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add canonical numeric fixtures for Phase 2</name>
  <files>test/fixtures/phase-02/labels.canonical.json, test/fixtures/phase-02/shapes.canonical.json, test/fixtures/phase-02/containers.canonical.json, test/label-fitting.test.ts, test/shape-geometry.test.ts, test/container-geometry.test.ts</files>
  <read_first>
    - `src/serialization/canonical.ts` and `test/serialization.test.ts` for fixture style.
    - `.planning/phases/02-text-labels-and-shape-geometry/02-VALIDATION.md` Wave 0 fixture directory requirement.
    - `.planning/phases/02-text-labels-and-shape-geometry/02-PATTERNS.md` `test/fixtures/phase-02/*.canonical.json` guidance.
    - Existing Phase 2 tests from prior plans.
  </read_first>
  <action>
    Create `test/fixtures/phase-02/`. Add three committed fixture files generated from `stringifyCanonical()` with default precision 3: `labels.canonical.json`, `shapes.canonical.json`, and `containers.canonical.json`. Update `test/label-fitting.test.ts`, `test/shape-geometry.test.ts`, and `test/container-geometry.test.ts` to compare their representative records to the committed fixture contents using `readFileSync(new URL("./fixtures/phase-02/<name>.canonical.json", import.meta.url), "utf8")` or an equivalent Node ESM-safe path. Fixture content must be numeric layout/geometry records only. Do not use SVG, HTML, Excalidraw, Mermaid, draw.io, screenshots, or visual snapshots.
  </action>
  <acceptance_criteria>
    - `test/fixtures/phase-02/labels.canonical.json` exists and contains `"lines"`, `"box"`, and `"contentBox"`.
    - `test/fixtures/phase-02/shapes.canonical.json` exists and contains entries for all seven supported shape names.
    - `test/fixtures/phase-02/containers.canonical.json` exists and contains `"childBounds"`, `"contentBox"`, `"anchors"`, and `"obstacleBox"`.
    - At least one Phase 2 test reads each fixture file and compares against `stringifyCanonical()`.
    - Fixture files are formatted as canonical JSON with trailing newline.
    - `rtk npm test -- label-fitting shape-geometry container-geometry` passes.
  </acceptance_criteria>
  <verify>
    <automated>rtk npm test -- label-fitting shape-geometry container-geometry</automated>
  </verify>
  <done>Canonical numeric fixtures lock Phase 2 label, shape, and container outputs without renderer snapshots.</done>
</task>

<task type="auto">
  <name>Task 3: Run renderer-neutral and full verification gates</name>
  <files>src/index.ts, test/public-api.test.ts, test/fixtures/phase-02/labels.canonical.json, test/fixtures/phase-02/shapes.canonical.json, test/fixtures/phase-02/containers.canonical.json</files>
  <read_first>
    - `.planning/phases/02-text-labels-and-shape-geometry/02-CONTEXT.md` decisions D-11, D-15, D-16, D-17 and deferred ideas.
    - `.planning/phases/02-text-labels-and-shape-geometry/02-VALIDATION.md` full suite command and final gate.
    - `package.json` scripts.
    - All Phase 2 test files.
  </read_first>
  <action>
    Run the focused Phase 2 command, typecheck, renderer-creep grep, and full verify. If grep finds renderer terms in core Phase 2 code, remove or rewrite them unless they are only in planning docs. The grep target is `src/text`, `src/labels`, `src/geometry`, and Phase 2 tests/fixtures, not `.planning`. Do not add UI-SPEC, SVG goldens, visual snapshots, Dagre layout, routing, DSL, CLI, or exporter code in this plan.
  </action>
  <acceptance_criteria>
    - `rtk npm test -- text-measurer label-fitting box-geometry shape-geometry container-geometry public-api serialization` passes.
    - `rtk npm run typecheck` passes.
    - `rtk npm run verify` passes.
    - `rtk bash -lc "! rg -n -i 'svg|html|css|excalidraw|draw\\.io|drawio|mermaid|<svg|<tspan|<text|path d=' src/text src/labels src/geometry test/text-measurer.test.ts test/label-fitting.test.ts test/box-geometry.test.ts test/shape-geometry.test.ts test/container-geometry.test.ts test/fixtures/phase-02"` exits 0, proving there are no renderer-output matches except assertions whose explicit purpose is forbidding those terms.
    - No files for Dagre layout, routing, DSL, CLI, SVG exporter, Excalidraw exporter, or UI are created.
  </acceptance_criteria>
  <verify>
    <automated>rtk npm test -- text-measurer label-fitting box-geometry shape-geometry container-geometry public-api serialization</automated>
    <automated>rtk npm run typecheck</automated>
    <automated>rtk bash -lc "! rg -n -i 'svg|html|css|excalidraw|draw\\.io|drawio|mermaid|&lt;svg|&lt;tspan|&lt;text|path d=' src/text src/labels src/geometry test/text-measurer.test.ts test/label-fitting.test.ts test/box-geometry.test.ts test/shape-geometry.test.ts test/container-geometry.test.ts test/fixtures/phase-02"</automated>
    <automated>rtk npm run verify</automated>
  </verify>
  <done>All Phase 2 requirements are reachable through public APIs, numeric fixtures are stable, renderer boundary checks pass, and the full project verify gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package root -> public consumers | Public imports expose Phase 2 APIs to downstream layout/routing/export phases. |
| fixture generation -> test suite | Canonical fixtures lock numeric behavior for future changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-19 | Tampering | `src/index.ts` public exports | mitigate | Public API test imports all representative Phase 2 APIs from root and package export check preserves root-only exports. |
| T-02-20 | Repudiation | canonical fixtures | mitigate | Compare fixtures generated by `stringifyCanonical()` so numeric drift is visible in tests. |
| T-02-21 | Elevation of Privilege | renderer creep | mitigate | Final grep gate blocks renderer-native fields and terms in core Phase 2 modules/tests. |
| T-02-22 | Denial of Service | full verification | mitigate | Use existing fast Vitest/typecheck/build/lint scripts; do not add browser, visual, or Canvas-only gates to default verify. |
</threat_model>

<verification>
Run these after the plan:

```bash
rtk npm test -- text-measurer label-fitting box-geometry shape-geometry container-geometry public-api serialization
rtk npm run typecheck
rtk npm run verify
```
</verification>

<success_criteria>
- Every Phase 2 requirement ID appears in this plan and at least one earlier feature plan.
- Public root exports include text, labels, and geometry barrels.
- Canonical fixture files exist and are asserted by tests.
- Renderer-neutral boundary and full `rtk npm run verify` are green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-text-labels-and-shape-geometry/02-05-SUMMARY.md`.
</output>
