---
phase: 02-text-labels-and-shape-geometry
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - src/geometry/containers.ts
  - src/geometry/index.ts
  - test/container-geometry.test.ts
autonomous: true
requirements:
  - TXT-02
  - GEO-01
  - GEO-02
requirements_addressed:
  - TXT-02
  - GEO-01
  - GEO-02
must_haves:
  truths:
    - "User can compute container geometry from known child boxes, padding, optional label/header, and minimum size."
    - "Container geometry returns outer box, content box, child bounds, anchors, obstacle box, and diagnostics without moving children."
    - "Container calculation uses the same label layout and box utility chain as normal nodes."
  artifacts:
    - path: "src/geometry/containers.ts"
      provides: "ContainerGeometryInput, ContainerGeometry, and computeContainerGeometry"
    - path: "test/container-geometry.test.ts"
      provides: "Container geometry tests for known child boxes and optional label layout"
  key_links:
    - from: "src/geometry/containers.ts"
      to: "src/geometry/boxes.ts"
      via: "unionBoxes, expandBox, normalizeInsets"
      pattern: "unionBoxes"
    - from: "src/geometry/containers.ts"
      to: "src/geometry/shapes.ts"
      via: "rectangle-like anchors or computeShapeGeometry"
      pattern: "computeShapeGeometry"
    - from: "src/geometry/containers.ts"
      to: "src/labels/types.ts"
      via: "optional LabelLayout"
      pattern: "LabelLayout"
---

<objective>
Create container geometry from known child boxes.

Purpose: Satisfy D-12 through D-14 and the overlapping TXT-02/GEO-01/GEO-02 requirement surface by aggregating known child boxes into renderer-neutral group/container geometry.
Output: Container geometry module and focused tests proving no automatic child placement or containment solving occurs.
</objective>

<execution_context>
@/home/zhangyangrui/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-text-labels-and-shape-geometry/02-CONTEXT.md
@.planning/phases/02-text-labels-and-shape-geometry/02-RESEARCH.md
@.planning/phases/02-text-labels-and-shape-geometry/02-VALIDATION.md
@.planning/phases/02-text-labels-and-shape-geometry/02-PATTERNS.md
@.planning/phases/02-text-labels-and-shape-geometry/02-02-SUMMARY.md
@.planning/phases/02-text-labels-and-shape-geometry/02-03-SUMMARY.md
@src/geometry/boxes.ts
@src/geometry/shapes.ts
@src/labels/types.ts
@src/ir/elements.ts
@src/ir/geometry.ts
@src/ir/diagnostics.ts

<interfaces>
Create container contracts:
```typescript
export interface ContainerGeometryInput {
	id: string;
	childBoxes: readonly Box[];
	padding: Insets | number;
	labelLayout?: LabelLayout;
	minSize?: Partial<Size>;
	obstacleMargin?: number | Insets;
}

export interface ContainerGeometry {
	id: string;
	box: Box;
	contentBox: Box;
	childBounds: Box;
	labelLayout?: LabelLayout;
	anchors: AnchorPoint[];
	obstacleBox: Box;
	diagnostics: Diagnostic[];
}

export function computeContainerGeometry(input: ContainerGeometryInput): ContainerGeometry;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED container geometry tests</name>
  <files>test/container-geometry.test.ts</files>
  <read_first>
    - `.planning/phases/02-text-labels-and-shape-geometry/02-CONTEXT.md` decisions D-12, D-13, D-14.
    - `.planning/phases/02-text-labels-and-shape-geometry/02-VALIDATION.md` Wave 0 entry for `test/container-geometry.test.ts`.
    - `.planning/phases/02-text-labels-and-shape-geometry/02-PATTERNS.md` entries for `src/geometry/containers.ts` and `test/container-geometry.test.ts`.
    - `src/geometry/boxes.ts`, `src/geometry/shapes.ts`, and `src/labels/types.ts` from prior plans.
  </read_first>
  <behavior>
    - RED test: child boxes `{ x: 40, y: 50, width: 100, height: 60 }` and `{ x: 180, y: 120, width: 80, height: 40 }` with padding `20` produce child bounds `{ x: 40, y: 50, width: 220, height: 110 }` and outer box expanded by padding.
    - RED test: optional `labelLayout` reserves header height above child content and is returned unchanged.
    - RED test: `minSize` enlarges the outer box without moving or mutating child boxes.
    - RED test: anchors and obstacle box are computed for the container outer box.
    - RED test: empty child boxes, non-finite boxes, invalid padding, and invalid min size throw `TypeError`.
    - RED test: no child placement happens; original child boxes remain byte-identical after computation.
  </behavior>
  <action>
    Create `test/container-geometry.test.ts` importing `computeContainerGeometry`, `DeterministicTextMeasurer`, `fitLabel`, and `stringifyCanonical` from `../src/index.js`. Use a precomputed `LabelLayout` from `fitLabel()` for the label/header case. Assert numeric boxes directly and serialize a result through `stringifyCanonical()` to prove deterministic fixture readiness. Do not import Dagre, constraints, routing, SVG, HTML, or exporters.
  </action>
  <acceptance_criteria>
    - `test/container-geometry.test.ts` exists and imports from `../src/index.js`.
    - Tests cover known child boxes, padding, optional label layout, min size, anchors, obstacle box, invalid input, and no child mutation.
    - The test file contains `computeContainerGeometry` and does not contain `dagre`, `route`, `constraint`, `<svg`, or exporter names.
    - Running `rtk npm test -- container-geometry` fails before implementation because container APIs do not exist yet.
  </acceptance_criteria>
  <verify>
    <automated>rtk npm test -- container-geometry</automated>
  </verify>
  <done>RED tests specify container geometry from known child boxes without auto-placement or containment solving.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement computeContainerGeometry</name>
  <files>src/geometry/containers.ts, src/geometry/index.ts, test/container-geometry.test.ts</files>
  <read_first>
    - `src/geometry/boxes.ts` for `normalizeInsets`, `unionBoxes`, `expandBox`, and validation.
    - `src/geometry/shapes.ts` for rectangle-like anchors/obstacle strategy.
    - `src/labels/types.ts` for `LabelLayout`.
    - `src/ir/diagnostics.ts` for `Diagnostic`.
    - `.planning/phases/02-text-labels-and-shape-geometry/02-RESEARCH.md` Pattern 5.
    - `test/container-geometry.test.ts` from Task 1.
  </read_first>
  <action>
    Create `src/geometry/containers.ts` exporting `ContainerGeometryInput`, `ContainerGeometry`, and `computeContainerGeometry`. Compute `childBounds = unionBoxes(input.childBoxes)`, normalize padding, reserve `headerHeight = input.labelLayout?.fittedSize.height ?? input.labelLayout?.box.height ?? 0`, and create an outer box that contains child bounds plus padding and header. Apply `minSize` by increasing width/height without translating children. Set `contentBox` to the padded child content area below the optional header. Compute anchors from the outer box using the same anchor names as shapes, and compute `obstacleBox = expandBox(outerBox, input.obstacleMargin ?? 0)`. Return diagnostics as an empty array unless a recoverable condition is explicitly tested. Update `src/geometry/index.ts` to export `containers.js`. Do not accept raw `Label` plus fitter in this plan; container geometry accepts precomputed `LabelLayout` to avoid circular dependency.
  </action>
  <acceptance_criteria>
    - `src/geometry/containers.ts` exports `ContainerGeometryInput`, `ContainerGeometry`, and `computeContainerGeometry`.
    - `computeContainerGeometry` calls or reuses `unionBoxes`, `normalizeInsets`, and `expandBox`.
    - Container geometry accepts `labelLayout?: LabelLayout` and does not import `fitLabel`, `LabelFitter`, or `@chenglou/pretext`.
    - No code in `src/geometry/containers.ts` mutates child boxes or changes child coordinates.
    - No code imports Dagre, constraint solvers, routing modules, SVG, HTML, or exporters.
    - `src/geometry/index.ts` exports `containers.js`.
    - `rtk npm test -- container-geometry box-geometry shape-geometry label-fitting` passes.
    - `rtk npm run typecheck` passes.
  </acceptance_criteria>
  <verify>
    <automated>rtk npm test -- container-geometry box-geometry shape-geometry label-fitting</automated>
    <automated>rtk npm run typecheck</automated>
    <automated>rtk bash -lc "! rg -n -i 'dagre|constraint|router|routing|svg|html|css|excalidraw|draw\\.io|drawio|mermaid' src/geometry/containers.ts test/container-geometry.test.ts"</automated>
  </verify>
  <done>Container geometry is implemented from known child boxes, label layout, padding, min size, anchors, and obstacle boxes without solving child placement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> computeContainerGeometry | Public callers pass child boxes, padding, minSize, label layout, and obstacle margins. |
| labels -> containers | A precomputed label layout crosses into container header sizing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15 | Tampering | `computeContainerGeometry` | mitigate | Validate child boxes, padding, minSize, and obstacle margins using box utility validation before computing aggregate geometry. |
| T-02-16 | Denial of Service | child aggregation | mitigate | Use a single pass union over known child boxes; reject empty arrays rather than attempting solver behavior. |
| T-02-17 | Elevation of Privilege | container scope creep | mitigate | Acceptance criteria forbid Dagre, constraint, routing, and renderer imports in container geometry. |
| T-02-18 | Repudiation | no child placement boundary | mitigate | Tests assert input child boxes remain unchanged and no containment solving occurs. |
</threat_model>

<verification>
Run these after the plan:

```bash
rtk npm test -- container-geometry box-geometry shape-geometry label-fitting
rtk npm run typecheck
rtk bash -lc "! rg -n -i 'dagre|constraint|router|routing|svg|html|css|excalidraw|draw\\.io|drawio|mermaid' src/geometry/containers.ts test/container-geometry.test.ts"
```
</verification>

<success_criteria>
- TXT-02, GEO-01, and GEO-02 appear in frontmatter and are covered by container tests.
- Container geometry computes boxes, anchors, and obstacle boxes from known child boxes.
- Container code uses precomputed label layout and does not solve child placement.
- Scope remains renderer-neutral and layout-solver-free.
</success_criteria>

<output>
After completion, create `.planning/phases/02-text-labels-and-shape-geometry/02-04-SUMMARY.md`.
</output>
