---
phase: 15
plan: 01
type: implementation
wave: 1
depends_on:
  - 14
files_modified:
  - src/solver/solve.ts
  - src/ir/label-layout.ts
  - src/ir/diagram.ts
  - test/solver.test.ts
  - test/dense-acceptance.test.ts
autonomous: true
requirements:
  - EXT-01
  - EXT-02
  - EXT-03
  - EXT-04
---

# Phase 15 Plan 01: External Label Execution

<objective>
Execute the external-label remediation path by converting congested inline edge labels into deterministic keyed callouts.
</objective>

<threat_model>
No external IO. Primary risks are breaking exporter assumptions about text annotations and hiding labels without preserving metadata.
</threat_model>

<tasks>
<task id="T15-01" type="execute">
  <read_first>
    - `src/solver/solve.ts`
    - `src/ir/label-layout.ts`
    - `src/ir/diagram.ts`
    - `.planning/phases/15-external-label-execution/15-RESEARCH.md`
  </read_first>
  <action>Add solver-side external label execution helpers that select congested edge labels, assign deterministic short keys, and create external callout annotation metadata. Preserve each source edge ID, original label text, key, shelf side, and callout box in output metadata or text annotations.</action>
  <acceptance_criteria>
    - Externalized labels include stable key and original text metadata.
    - Repeated solves of the same fixture produce identical keys and callout order.
    - `rtk npm test -- test/determinism.test.ts test/solver.test.ts` exits 0.
  </acceptance_criteria>
</task>

<task id="T15-02" type="execute">
  <read_first>
    - `src/solver/solve.ts`
    - `test/dense-acceptance.test.ts`
  </read_first>
  <action>When `remediationPolicy.externalLabels` or equivalent is `auto`, apply externalization to edge labels involved in label congestion or route-label-loop exhaustion before final route/text clearance. Long label boxes must not remain local route-clearance obstacles after externalization.</action>
  <acceptance_criteria>
    - Dense CV fixture with external-label auto has fewer or zero route/edge-label intersections than suggest/off mode.
    - Externalized long label annotations are skipped by local route-clearance checks.
    - `rtk npm test -- test/dense-acceptance.test.ts` exits 0.
  </acceptance_criteria>
</task>

<task id="T15-03" type="execute">
  <read_first>
    - `src/solver/solve.ts`
    - `src/ir/diagram.ts`
    - `test/solver.test.ts`
  </read_first>
  <action>Update remediation plan output so applied external labels produce an `external-label` plan with status `applied`, affected `edgeIds`, key mapping, and shelf bounds. Preserve suggest-only behavior when policy is `suggest` or off.</action>
  <acceptance_criteria>
    - Auto mode emits an applied external-label remediation plan.
    - Suggest mode emits a suggested plan without changing geometry.
    - Off mode does not externalize labels automatically.
  </acceptance_criteria>
</task>

<task id="T15-04" type="tdd">
  <read_first>
    - `test/solver.test.ts`
    - `test/dense-acceptance.test.ts`
    - `src/solver/solve.ts`
  </read_first>
  <action>Add tests for deterministic keys, shelf allocation, route/text clearance improvement, and preservation of original edge label text for downstream consumers.</action>
  <acceptance_criteria>
    - Tests assert exact stable keys for at least three externalized edges.
    - Tests assert original label text remains discoverable in output.
    - `rtk npm test -- test/solver.test.ts test/dense-acceptance.test.ts` exits 0.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- `rtk npm test -- test/solver.test.ts test/dense-acceptance.test.ts test/determinism.test.ts`
- `rtk npm run typecheck`
</verification>

<success_criteria>
- External-label auto execution is deterministic and machine-readable.
- Long inline label congestion can be removed from dense route fields.
- Suggest/off modes remain available.
</success_criteria>
