# Phase 10 Plan 01: External Label Semantics

## Goal

Make edge-label congestion actionable and expose external-callout-required solver output.

## Tasks

1. Add edge-label placement metadata to solved text annotations.
2. Add `externalLabels` solver option and YAML DSL forwarding.
3. Emit `routing.label-externalization.required` diagnostics.
4. Keep route/text clearance distinct for local labels vs externalized labels.
5. Add tests for API, DSL, and strict remediation behavior.

## Verification

- `npm test -- test/solver.test.ts test/dsl.test.ts`
- `npm run typecheck`
