---
phase: 03-layout-constraints-and-routing
plan: 04
type: execute
wave: 2
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - src/solver/index.ts
  - src/solver/solve.ts
  - test/solver.test.ts
  - test/determinism.test.ts
autonomous: true
requirements: [LAY-01, LAY-02, LAY-03, LAY-04, RTE-01, RTE-02, RTE-03]
must_haves:
  truths:
    - "User can call one solver entrypoint to produce a CoordinatedDiagram from NormalizedDiagram."
    - "Solver output includes coordinated nodes with boxes and anchors, edges with routed points, groups with boxes, bounds, and diagnostics."
    - "Solver preserves fixed/exact locks, applies constraints, routes edges, and returns partial coordinated diagrams plus diagnostics when safe."
    - "Repeated solver calls on equivalent input serialize identically."
  artifacts:
    - path: "src/solver/solve.ts"
      provides: "Integrated solveDiagram coordinator"
      exports: ["solveDiagram"]
    - path: "src/solver/index.ts"
      provides: "Solver module barrel"
      contains: "export * from \"./solve.js\""
    - path: "test/solver.test.ts"
      provides: "Integrated Phase 3 behavior tests"
      contains: "solveDiagram"
    - path: "test/determinism.test.ts"
      provides: "Repeated solve canonical equality tests"
      contains: "stringifyCanonical"
  key_links:
    - from: "src/solver/solve.ts"
      to: "src/layout/dagre.ts"
      via: "runDagreInitialLayout"
      pattern: "runDagreInitialLayout"
    - from: "src/solver/solve.ts"
      to: "src/constraints/solver.ts"
      via: "applyLayoutConstraints"
      pattern: "applyLayoutConstraints"
    - from: "src/solver/solve.ts"
      to: "src/routing/routes.ts"
      via: "routeEdge"
      pattern: "routeEdge"
    - from: "src/solver/solve.ts"
      to: "src/geometry/containers.ts"
      via: "computeContainerGeometry"
      pattern: "computeContainerGeometry"
---

<objective>
Wire layout, constraints, containers, shape geometry, and routing into a single renderer-neutral `solveDiagram()` entrypoint.

Purpose: This creates the coordinated IR contract that later exporters consume without recalculating layout or routing.
Output: `src/solver/*`, `test/solver.test.ts`, and `test/determinism.test.ts`.
</objective>

<execution_context>
@/home/zhangyangrui/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-layout-constraints-and-routing/03-CONTEXT.md
@.planning/phases/03-layout-constraints-and-routing/03-RESEARCH.md
@.planning/phases/03-layout-constraints-and-routing/03-VALIDATION.md
@.planning/phases/03-layout-constraints-and-routing/03-PATTERNS.md
@.planning/phases/02-text-labels-and-shape-geometry/02-05-SUMMARY.md
@src/ir/diagram.ts
@src/ir/elements.ts
@src/ir/constraints.ts
@src/ir/diagnostics.ts
@src/layout/dagre.ts
@src/constraints/solver.ts
@src/routing/routes.ts
@src/geometry/shapes.ts
@src/geometry/containers.ts
@src/geometry/boxes.ts
@src/serialization/canonical.ts

<interfaces>
Existing coordinated output contract:

```typescript
export interface CoordinatedDiagram {
  id: string;
  title?: string;
  direction: DiagramDirection;
  nodes: CoordinatedNode[];
  edges: CoordinatedEdge[];
  groups: CoordinatedGroup[];
  diagnostics: Diagnostic[];
  bounds: Box;
  metadata?: DiagramMetadata;
}
```

Create this solver entrypoint:

```typescript
export interface SolveDiagramOptions {
  routeKind?: RouteKind;
  obstacleMargin?: number | Insets;
}

export function solveDiagram(
  diagram: NormalizedDiagram,
  options?: SolveDiagramOptions,
): CoordinatedDiagram;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create integrated solver tests</name>
  <files>src/solver/index.ts, src/solver/solve.ts, test/solver.test.ts, test/determinism.test.ts</files>
  <read_first>
    - `src/solver/index.ts` if it exists; otherwise confirm the path is new with `rtk test ! -e src/solver/index.ts`
    - `src/solver/solve.ts` if it exists; otherwise confirm the path is new with `rtk test ! -e src/solver/solve.ts`
    - `test/solver.test.ts` if it exists; otherwise confirm the path is new with `rtk test ! -e test/solver.test.ts`
    - `test/determinism.test.ts` if it exists; otherwise confirm the path is new with `rtk test ! -e test/determinism.test.ts`
    - `src/ir/diagram.ts`
    - `src/layout/dagre.ts`
    - `src/constraints/solver.ts`
    - `src/routing/routes.ts`
    - `src/serialization/canonical.ts`
    - `.planning/phases/03-layout-constraints-and-routing/03-CONTEXT.md` decisions D-01 through D-22
    - `.planning/phases/03-layout-constraints-and-routing/03-VALIDATION.md` rows 03-04-01 and 03-05-01
    - `.planning/phases/03-layout-constraints-and-routing/03-PATTERNS.md` sections `src/solver/index.ts` and `src/solver/solve.ts`
  </read_first>
  <behavior>
    - Test 1: `solveDiagram()` returns a `CoordinatedDiagram` with finite node boxes, node anchors, routed edge points, group boxes, bounds, and diagnostics array.
    - Test 2: A fixed node `position` remains unchanged while automatic nodes still receive finite boxes.
    - Test 3: An impossible or malformed constraint returns a partial diagram plus `error` diagnostics without throwing when remaining coordinates are safe.
    - Test 4: Repeated `stringifyCanonical(solveDiagram(input))` calls return byte-identical strings.
  </behavior>
  <action>Create `src/solver/index.ts` exporting `./solve.js`. Create a placeholder `src/solver/solve.ts` with exported `SolveDiagramOptions` and `solveDiagram()` signature if needed for tests to compile. Create `test/solver.test.ts` with integrated fixture inputs covering Dagre layout, fixed-position hybrid layout, constraints, containment group output, default orthogonal edge routing, straight routing through `options.routeKind = "straight"`, and diagnostic propagation. Create `test/determinism.test.ts` comparing two repeated `stringifyCanonical(solveDiagram(input))` calls for byte equality. The tests must import from `../src/solver/index.js` until Plan 05 wires the root export.</action>
  <acceptance_criteria>
    - `rtk rg -n 'solveDiagram' src/solver/solve.ts test/solver.test.ts test/determinism.test.ts` finds solver references in implementation and tests.
    - `rtk rg -n 'routeKind.*straight|orthogonal|position' test/solver.test.ts` finds integrated straight/default orthogonal/fixed-position assertions.
    - `rtk rg -n 'stringifyCanonical' test/determinism.test.ts` finds deterministic serialization assertions.
    - `rtk npm test -- solver determinism` initially fails before full implementation, then passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>rtk npm test -- solver determinism</automated>
  </verify>
  <done>Integrated tests define the solveDiagram contract and deterministic coordinated output behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement solveDiagram coordinator</name>
  <files>src/solver/solve.ts, test/solver.test.ts, test/determinism.test.ts</files>
  <read_first>
    - `src/solver/solve.ts`
    - `test/solver.test.ts`
    - `test/determinism.test.ts`
    - `src/ir/diagram.ts`
    - `src/ir/elements.ts`
    - `src/layout/dagre.ts`
    - `src/constraints/solver.ts`
    - `src/routing/routes.ts`
    - `src/geometry/shapes.ts`
    - `src/geometry/containers.ts`
    - `src/geometry/boxes.ts`
    - `.planning/phases/03-layout-constraints-and-routing/03-RESEARCH.md` section "System Architecture Diagram"
    - `.planning/phases/03-layout-constraints-and-routing/03-CONTEXT.md` decisions D-02, D-05, D-10, D-12, D-14, D-15, D-17, D-20
  </read_first>
  <behavior>
    - Test 1: Solver order is prepare stable maps, Dagre initial boxes, fixed/exact/constraints, groups/containers, shape geometry, route edges, build coordinated diagram.
    - Test 2: Groups are computed from solved child boxes with `computeContainerGeometry()`.
    - Test 3: Fixed containers diagnose children outside padded content with `constraints.containment.impossible`.
    - Test 4: Missing edge node references emit `solver.edge-reference.missing` and omit unsafe edge routes.
  </behavior>
  <action>Implement `solveDiagram(diagram, options)` in `src/solver/solve.ts`. Stable-sort nodes, edges, groups, and constraints by `id` before preparation. Build `DagreLayoutInput` from `NormalizedNode.size` and `NormalizedEdge.source/target` and call `runDagreInitialLayout()`. Pass initial boxes, nodes, groups, and constraints into `applyLayoutConstraints()`. For every solved node box, call `computeShapeGeometry({ shape: node.shape, box, obstacleMargin: options?.obstacleMargin ?? 0 })` and emit `CoordinatedNode` with anchors. For groups, gather solved child node and child group boxes and call `computeContainerGeometry({ id, childBoxes, padding: group.padding, obstacleMargin })`; for empty or missing child references, add `solver.group-reference.missing` and skip unsafe group boxes. For edges, validate source/target node IDs against coordinated node geometry; route with `routeEdge({ kind: options?.routeKind ?? "orthogonal", direction: diagram.direction, source, target, sourceAnchor: edge.source.anchor, targetAnchor: edge.target.anchor, obstacles })`; append route diagnostics with edge ID detail. Compute diagram `bounds` as union of coordinated node and group boxes. Return a `CoordinatedDiagram` preserving `id`, `title`, `direction`, `metadata`, prior `diagram.diagnostics`, and all Phase 3 diagnostics. Do not call Pretext or use renderer/exporter fields.</action>
  <acceptance_criteria>
    - `rtk rg -n 'runDagreInitialLayout|applyLayoutConstraints|computeContainerGeometry|computeShapeGeometry|routeEdge|unionBoxes' src/solver/solve.ts` finds every integration call.
    - `rtk rg -n 'solver\\.edge-reference\\.missing|solver\\.group-reference\\.missing|constraints\\.containment\\.impossible' src/solver/solve.ts test/solver.test.ts` finds stable diagnostics.
    - `rtk rg -n -i 'svg|html|css|excalidraw|draw\\.io|drawio|mermaid|<svg|<text|path d=' src/solver test/solver.test.ts test/determinism.test.ts` returns no matches.
    - `rtk npm test -- solver determinism` passes.
    - `rtk npm test -- layout constraints routing solver determinism` passes.
  </acceptance_criteria>
  <verify>
    <automated>rtk npm test -- layout constraints routing solver determinism</automated>
  </verify>
  <done>`solveDiagram()` produces renderer-neutral coordinated IR with nodes, groups, routes, bounds, diagnostics, and deterministic canonical output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NormalizedDiagram -> solveDiagram | Full prepared diagram input crosses into coordinated geometry construction. |
| Partial diagnostics -> downstream exporters | Later phases will consume coordinated output and diagnostics from this solver. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `src/solver/solve.ts` | mitigate | Reject or diagnose non-finite boxes from layout/constraints/routing before adding nodes, groups, edges, or bounds to coordinated IR. |
| T-03-02 | Tampering | `src/solver/solve.ts` | mitigate | Validate edge, group, child, and constraint references before routing or container construction; emit `solver.*.missing` diagnostics and omit unsafe elements. |
| T-03-03 | Denial of Service | `src/solver/solve.ts` | mitigate | Stable-sort input once, call Dagre once, route each edge with bounded candidates, and avoid nested unbounded search. |
</threat_model>

<verification>
Run `rtk npm test -- solver determinism` after Task 1 and `rtk npm test -- layout constraints routing solver determinism` after Task 2. At wave end, run `rtk npm run typecheck && rtk npm test -- layout constraints routing solver determinism && rtk npm run lint`.
</verification>

<success_criteria>
- `solveDiagram()` produces a full `CoordinatedDiagram`.
- Fixed positions and exact-position constraints survive integrated solving.
- Constraints and routing diagnostics appear in final `diagnostics`.
- Repeated solves serialize byte-identically with `stringifyCanonical()`.
- Solver code remains renderer-neutral.
</success_criteria>

<output>
After completion, create `.planning/phases/03-layout-constraints-and-routing/03-04-SUMMARY.md`.
</output>
