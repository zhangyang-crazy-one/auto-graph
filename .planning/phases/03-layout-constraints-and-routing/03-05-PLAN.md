---
phase: 03-layout-constraints-and-routing
plan: 05
type: execute
wave: 3
depends_on: [03-04]
files_modified:
  - src/index.ts
  - test/public-api.test.ts
  - test/determinism.test.ts
  - test/fixtures/phase-03/dagre-directions.canonical.json
  - test/fixtures/phase-03/hybrid-layout.canonical.json
  - test/fixtures/phase-03/constraints.canonical.json
  - test/fixtures/phase-03/routing.canonical.json
  - biome.json
autonomous: true
requirements: [LAY-01, LAY-02, LAY-03, LAY-04, RTE-01, RTE-02, RTE-03]
must_haves:
  truths:
    - "Developer can import Phase 3 layout, constraints, routing, and solver APIs from the package root."
    - "Canonical Phase 3 coordinated fixtures exist and are compared against solver output."
    - "Focused Phase 3 tests, typecheck, lint, renderer-neutral grep, and full verify all pass."
  artifacts:
    - path: "src/index.ts"
      provides: "Root public exports for Phase 3 modules"
      contains: "export * from \"./solver/index.js\""
    - path: "test/public-api.test.ts"
      provides: "Root import proof for Phase 3 APIs"
      contains: "solveDiagram"
    - path: "test/fixtures/phase-03/dagre-directions.canonical.json"
      provides: "Canonical coordinated fixture for Dagre directions"
      min_lines: 5
    - path: "test/fixtures/phase-03/hybrid-layout.canonical.json"
      provides: "Canonical coordinated fixture for fixed/auto hybrid layout"
      min_lines: 5
    - path: "test/fixtures/phase-03/constraints.canonical.json"
      provides: "Canonical coordinated fixture for constraints and diagnostics"
      min_lines: 5
    - path: "test/fixtures/phase-03/routing.canonical.json"
      provides: "Canonical coordinated fixture for routing"
      min_lines: 5
  key_links:
    - from: "src/index.ts"
      to: "./layout/index.js"
      via: "root export"
    - from: "src/index.ts"
      to: "./solver/index.js"
      via: "root export"
    - from: "test/determinism.test.ts"
      to: "test/fixtures/phase-03/*.canonical.json"
      via: "readFileSync fixture comparison"
      pattern: "fixtures/phase-03"
---

<objective>
Publish Phase 3 APIs through the root entrypoint and lock coordinated output with canonical fixtures and full validation gates.

Purpose: This makes Phase 3 consumable by Phase 4 exporters while proving deterministic, renderer-neutral behavior.
Output: root exports, public API tests, Phase 3 canonical fixtures, fixture comparison tests, and full verification evidence.
</objective>

<execution_context>
@/home/zhangyangrui/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-layout-constraints-and-routing/03-CONTEXT.md
@.planning/phases/03-layout-constraints-and-routing/03-RESEARCH.md
@.planning/phases/03-layout-constraints-and-routing/03-VALIDATION.md
@.planning/phases/03-layout-constraints-and-routing/03-PATTERNS.md
@.planning/phases/02-text-labels-and-shape-geometry/02-05-SUMMARY.md
@src/index.ts
@src/layout/index.ts
@src/constraints/index.ts
@src/routing/index.ts
@src/solver/index.ts
@src/solver/solve.ts
@test/public-api.test.ts
@test/determinism.test.ts
@test/fixtures/phase-02/containers.canonical.json
@biome.json

<interfaces>
Root package entrypoint pattern:

```typescript
export * from "./geometry/index.js";
export * from "./ir/index.js";
export * from "./labels/index.js";
export * from "./serialization/index.js";
export * from "./text/index.js";
```

Add these Phase 3 exports:

```typescript
export * from "./layout/index.js";
export * from "./constraints/index.js";
export * from "./routing/index.js";
export * from "./solver/index.js";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire root public exports and public API proof</name>
  <files>src/index.ts, test/public-api.test.ts</files>
  <read_first>
    - `src/index.ts`
    - `test/public-api.test.ts`
    - `src/layout/index.ts`
    - `src/constraints/index.ts`
    - `src/routing/index.ts`
    - `src/solver/index.ts`
    - `.planning/phases/03-layout-constraints-and-routing/03-CONTEXT.md` decisions D-02, D-03, D-22
    - `.planning/phases/03-layout-constraints-and-routing/03-PATTERNS.md` sections `src/index.ts` and `test/public-api.test.ts`
    - `.planning/phases/02-text-labels-and-shape-geometry/02-05-SUMMARY.md` patterns-established
  </read_first>
  <action>Update `src/index.ts` to export `./layout/index.js`, `./constraints/index.js`, `./routing/index.js`, and `./solver/index.js` in addition to existing Phase 2 barrels. Extend `test/public-api.test.ts` to import `runDagreInitialLayout`, `applyLayoutConstraints`, `routeEdge`, `simplifyRoute`, and `solveDiagram` from `../src/index.js`. Add one public API test that constructs a small `NormalizedDiagram`, calls `solveDiagram()`, and asserts that `nodes[0].box`, `edges[0].points`, and `diagnostics` exist. Keep package exports root-only in `package.json`; do not add subpath exports.</action>
  <acceptance_criteria>
    - `rtk rg -n 'export \\* from \"\\./layout/index\\.js\"|export \\* from \"\\./constraints/index\\.js\"|export \\* from \"\\./routing/index\\.js\"|export \\* from \"\\./solver/index\\.js\"' src/index.ts` finds all four exports.
    - `rtk rg -n 'runDagreInitialLayout|applyLayoutConstraints|routeEdge|simplifyRoute|solveDiagram' test/public-api.test.ts` finds root import proof for all Phase 3 APIs.
    - `rtk node -e "const pkg=require('./package.json'); if (Object.keys(pkg.exports).join(',') !== '.') process.exit(1)"` exits 0.
    - `rtk npm test -- public-api solver` passes.
  </acceptance_criteria>
  <verify>
    <automated>rtk npm test -- public-api solver</automated>
  </verify>
  <done>Phase 3 APIs are available from the root package entrypoint without adding package subpath exports.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 3 canonical coordinated fixtures</name>
  <files>test/determinism.test.ts, test/fixtures/phase-03/dagre-directions.canonical.json, test/fixtures/phase-03/hybrid-layout.canonical.json, test/fixtures/phase-03/constraints.canonical.json, test/fixtures/phase-03/routing.canonical.json, biome.json</files>
  <read_first>
    - `test/determinism.test.ts`
    - `test/fixtures/phase-03/dagre-directions.canonical.json` if it exists; otherwise confirm the path is new with `rtk test ! -e test/fixtures/phase-03/dagre-directions.canonical.json`
    - `test/fixtures/phase-03/hybrid-layout.canonical.json` if it exists; otherwise confirm the path is new with `rtk test ! -e test/fixtures/phase-03/hybrid-layout.canonical.json`
    - `test/fixtures/phase-03/constraints.canonical.json` if it exists; otherwise confirm the path is new with `rtk test ! -e test/fixtures/phase-03/constraints.canonical.json`
    - `test/fixtures/phase-03/routing.canonical.json` if it exists; otherwise confirm the path is new with `rtk test ! -e test/fixtures/phase-03/routing.canonical.json`
    - `test/fixtures/phase-02/containers.canonical.json`
    - `src/serialization/canonical.ts`
    - `src/solver/solve.ts`
    - `biome.json`
    - `.planning/phases/03-layout-constraints-and-routing/03-CONTEXT.md` decisions D-21 and D-22
    - `.planning/phases/03-layout-constraints-and-routing/03-VALIDATION.md` Wave 0 Requirements and Test Infrastructure sections
    - `.planning/phases/02-text-labels-and-shape-geometry/02-05-SUMMARY.md` deviations about Biome fixture formatting
  </read_first>
  <action>Create `test/fixtures/phase-03/` and four committed canonical JSON fixtures named `dagre-directions.canonical.json`, `hybrid-layout.canonical.json`, `constraints.canonical.json`, and `routing.canonical.json`. Generate each fixture with `stringifyCanonical()` from solver outputs, not ad hoc JSON formatting. Extend `test/determinism.test.ts` to read each fixture with `readFileSync(new URL("./fixtures/phase-03/<name>.canonical.json", import.meta.url), "utf8")` and compare it to fresh `stringifyCanonical(solveDiagram(input))` output. Update `biome.json` so `test/fixtures/phase-03` is excluded from formatter rewrites exactly as Phase 2 fixture JSON is excluded. Fixture scenarios must cover Dagre direction output, fixed/auto hybrid layout, constraint diagnostics, and orthogonal routing.</action>
  <acceptance_criteria>
    - `rtk test -s test/fixtures/phase-03/dagre-directions.canonical.json` exits 0.
    - `rtk test -s test/fixtures/phase-03/hybrid-layout.canonical.json` exits 0.
    - `rtk test -s test/fixtures/phase-03/constraints.canonical.json` exits 0.
    - `rtk test -s test/fixtures/phase-03/routing.canonical.json` exits 0.
    - `rtk rg -n 'fixtures/phase-03|readFileSync|stringifyCanonical' test/determinism.test.ts` finds fixture comparison logic.
    - `rtk rg -n 'phase-03' biome.json` confirms fixture formatting exclusion.
    - `rtk npm test -- determinism solver` passes.
  </acceptance_criteria>
  <verify>
    <automated>rtk npm test -- determinism solver</automated>
  </verify>
  <done>Phase 3 canonical coordinated fixtures are committed and compared against fresh solver output.</done>
</task>

<task type="auto">
  <name>Task 3: Run Phase 3 full validation gates</name>
  <files>src/index.ts, test/public-api.test.ts, test/determinism.test.ts, biome.json</files>
  <read_first>
    - `src/index.ts`
    - `test/public-api.test.ts`
    - `test/layout.test.ts`
    - `test/constraints.test.ts`
    - `test/routing.test.ts`
    - `test/solver.test.ts`
    - `test/determinism.test.ts`
    - `biome.json`
    - `.planning/phases/03-layout-constraints-and-routing/03-VALIDATION.md`
    - `.planning/phases/03-layout-constraints-and-routing/03-CONTEXT.md` decisions D-03, D-06, D-19, D-22
  </read_first>
  <action>Run the complete Phase 3 validation sequence and fix only issues directly caused by Phase 3 implementation or fixture formatting. Required commands are `rtk npm run typecheck`, `rtk npm test -- layout constraints routing solver determinism`, `rtk npm run lint`, renderer-neutral grep `rtk rg -n -i "svg|html|css|excalidraw|draw\\.io|drawio|mermaid|<svg|<text|path d=" src/layout src/constraints src/routing src/solver test/layout.test.ts test/constraints.test.ts test/routing.test.ts test/solver.test.ts test/determinism.test.ts`, and `rtk npm run verify`. If the renderer grep finds only quoted forbidden terms inside the grep command in documentation, ignore that; any match inside Phase 3 source or tests must be removed unless it is a negative assertion that explicitly proves absence.</action>
  <acceptance_criteria>
    - `rtk npm run typecheck` exits 0.
    - `rtk npm test -- layout constraints routing solver determinism` exits 0.
    - `rtk npm run lint` exits 0.
    - `rtk rg -n -i "svg|html|css|excalidraw|draw\\.io|drawio|mermaid|<svg|<text|path d=" src/layout src/constraints src/routing src/solver test/layout.test.ts test/constraints.test.ts test/routing.test.ts test/solver.test.ts test/determinism.test.ts` returns no renderer creep matches in implementation or tests.
    - `rtk npm run verify` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>rtk npm run verify</automated>
  </verify>
  <done>Focused tests, typecheck, lint, renderer-neutral grep, and full verify all pass for Phase 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| root public API -> consumers | Public exports become the stable surface later phases import. |
| canonical fixtures -> regression checks | Committed JSON fixtures define deterministic coordinated output expectations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `test/fixtures/phase-03/*.canonical.json` | mitigate | Generate fixtures only from `stringifyCanonical(solveDiagram(input))`; canonical serializer rejects non-finite numbers. |
| T-03-02 | Tampering | `test/determinism.test.ts` | mitigate | Fixture scenarios include malformed references and diagnostic code assertions so corrupt references remain visible. |
| T-03-03 | Denial of Service | `rtk npm run verify` gate | mitigate | Full gate includes focused routing/solver tests that prove bounded candidate routing and no full grid A* references. |
</threat_model>

<verification>
Run `rtk npm run typecheck && rtk npm test -- layout constraints routing solver determinism && rtk npm run lint`, the renderer-neutral grep gate, and `rtk npm run verify`.
</verification>

<success_criteria>
- Root entrypoint exports layout, constraints, routing, and solver modules.
- Public API tests prove Phase 3 APIs are importable from `../src/index.js`.
- Four Phase 3 canonical fixtures exist under `test/fixtures/phase-03/`.
- Determinism tests compare fresh solver output against committed fixtures.
- Full `rtk npm run verify` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-layout-constraints-and-routing/03-05-SUMMARY.md`.
</output>
