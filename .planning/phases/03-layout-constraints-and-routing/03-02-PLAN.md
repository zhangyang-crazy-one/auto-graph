---
phase: 03-layout-constraints-and-routing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ir/elements.ts
  - src/constraints/index.ts
  - src/constraints/types.ts
  - src/constraints/solver.ts
  - test/constraints.test.ts
autonomous: true
requirements: [LAY-02, LAY-03, LAY-04]
must_haves:
  truths:
    - "User can keep fixed node positions and exact-position constraints as hard locks."
    - "User can apply exact-position, containment, relative-position, align, and distribute constraints in the locked precedence order."
    - "User receives stable diagnostics for missing references, non-finite coordinates, lock conflicts, containment failures, and unresolved overlaps."
  artifacts:
    - path: "src/ir/elements.ts"
      provides: "Normalized fixed-position support"
      contains: "position?: Point"
    - path: "src/constraints/solver.ts"
      provides: "Layered constraint solver"
      exports: ["applyLayoutConstraints"]
    - path: "src/constraints/types.ts"
      provides: "Constraint solver input/result contracts"
      exports: ["ConstraintSolverInput", "ConstraintSolverResult", "LayoutLock"]
    - path: "test/constraints.test.ts"
      provides: "LAY-02/LAY-03/LAY-04 tests"
      contains: "constraints.locked-target-not-moved"
  key_links:
    - from: "src/constraints/solver.ts"
      to: "src/ir/constraints.ts"
      via: "Constraint union handling"
      pattern: "ExactPositionConstraint|RelativePositionConstraint|AlignConstraint|DistributeConstraint|ContainmentConstraint"
    - from: "src/constraints/solver.ts"
      to: "src/geometry/boxes.ts"
      via: "finite box validation and AABB collision checks"
      pattern: "validateBox|intersectsAabb"
---

<objective>
Create the deterministic Phase 3 constraint layer used after Dagre initial placement and before routing.

Purpose: This enforces fixed/exact locks, applies the locked constraint precedence, and reports conflicts without silently corrupting coordinates.
Output: `src/constraints/*`, fixed-position support on `NormalizedNode`, and `test/constraints.test.ts`.
</objective>

<execution_context>
@/home/zhangyangrui/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-layout-constraints-and-routing/03-CONTEXT.md
@.planning/phases/03-layout-constraints-and-routing/03-RESEARCH.md
@.planning/phases/03-layout-constraints-and-routing/03-VALIDATION.md
@.planning/phases/03-layout-constraints-and-routing/03-PATTERNS.md
@.planning/phases/02-text-labels-and-shape-geometry/02-CONTEXT.md
@src/ir/elements.ts
@src/ir/constraints.ts
@src/ir/diagnostics.ts
@src/ir/geometry.ts
@src/geometry/boxes.ts

<interfaces>
Existing diagnostic contract:

```typescript
export interface Diagnostic {
  severity: "info" | "warning" | "error";
  code: string;
  message: string;
  path?: DiagnosticPathSegment[];
  detail?: JsonObject;
}
```

Create these constraint contracts:

```typescript
export type LayoutLockSource = "fixed-position" | "exact-position";

export interface LayoutLock {
  nodeId: string;
  source: LayoutLockSource;
}

export interface ConstraintSolverInput {
  boxes: ReadonlyMap<string, Box>;
  nodes: readonly NormalizedNode[];
  groups: readonly NormalizedGroup[];
  constraints: readonly Constraint[];
}

export interface ConstraintSolverResult {
  boxes: Map<string, Box>;
  locks: Map<string, LayoutLock>;
  diagnostics: Diagnostic[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fixed-position contract and constraint solver tests</name>
  <files>src/ir/elements.ts, src/constraints/index.ts, src/constraints/types.ts, test/constraints.test.ts</files>
  <read_first>
    - `src/ir/elements.ts`
    - `src/constraints/index.ts` if it exists; otherwise confirm the path is new with `rtk test ! -e src/constraints/index.ts`
    - `src/constraints/types.ts` if it exists; otherwise confirm the path is new with `rtk test ! -e src/constraints/types.ts`
    - `test/constraints.test.ts` if it exists; otherwise confirm the path is new with `rtk test ! -e test/constraints.test.ts`
    - `src/ir/constraints.ts`
    - `src/ir/diagnostics.ts`
    - `.planning/phases/03-layout-constraints-and-routing/03-CONTEXT.md` decisions D-07 through D-13
    - `.planning/phases/03-layout-constraints-and-routing/03-VALIDATION.md` rows 03-02-01 and 03-02-02
    - `.planning/phases/03-layout-constraints-and-routing/03-PATTERNS.md` sections `src/constraints/index.ts` and `src/constraints/types.ts`
  </read_first>
  <behavior>
    - Test 1: A `NormalizedNode` can carry `position?: Point`, and a fixed node keeps that exact x/y when constraints run.
    - Test 2: An `exact-position` constraint locks a node before weaker align/distribute constraints run.
    - Test 3: A missing constraint reference emits an `error` diagnostic code `constraints.reference.missing`.
  </behavior>
  <action>Extend `NormalizedNode` in `src/ir/elements.ts` with `position?: Point` so prepared diagrams can preserve fixed node coordinates from intent without adding exporter fields. Create `src/constraints/types.ts` with `LayoutLockSource`, `LayoutLock`, `ConstraintSolverInput`, and `ConstraintSolverResult` exactly as described in the interface block. Create `src/constraints/index.ts` exporting `./solver.js` and `./types.js`. Create `test/constraints.test.ts` with failing tests for fixed-position lock behavior, exact-position precedence over weaker constraints, and missing-reference diagnostics. Use diagnostic code strings `constraints.reference.missing`, `constraints.position.invalid`, `constraints.conflict.exact-position`, `constraints.locked-target-not-moved`, `constraints.containment.impossible`, and `constraints.overlap.unresolved` in test expectations where the behavior applies.</action>
  <acceptance_criteria>
    - `rtk rg -n 'position\\?: Point' src/ir/elements.ts` finds the NormalizedNode fixed-position field.
    - `rtk rg -n 'export interface ConstraintSolverInput|export interface ConstraintSolverResult|export interface LayoutLock' src/constraints/types.ts` finds all three exported contracts.
    - `rtk rg -n 'constraints\\.reference\\.missing|constraints\\.locked-target-not-moved' test/constraints.test.ts` finds stable diagnostic assertions.
    - `rtk npm test -- constraints` initially fails before `applyLayoutConstraints` exists, then passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>rtk npm test -- constraints</automated>
  </verify>
  <done>Fixed-position data has a renderer-neutral IR field and constraint tests define the required lock/diagnostic behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement layered constraint solving</name>
  <files>src/constraints/solver.ts, test/constraints.test.ts</files>
  <read_first>
    - `src/constraints/solver.ts` if it exists; otherwise confirm the path is new with `rtk test ! -e src/constraints/solver.ts`
    - `test/constraints.test.ts`
    - `src/constraints/types.ts`
    - `src/ir/constraints.ts`
    - `src/ir/elements.ts`
    - `src/geometry/boxes.ts`
    - `.planning/phases/03-layout-constraints-and-routing/03-CONTEXT.md` decisions D-07 through D-13
    - `.planning/phases/03-layout-constraints-and-routing/03-RESEARCH.md` sections "Pattern 3: Constraint Layering", "Pitfall 2", and "Pitfall 3"
    - `.planning/phases/03-layout-constraints-and-routing/03-PATTERNS.md` section `src/constraints/solver.ts`
  </read_first>
  <behavior>
    - Test 1: Precedence order is fixed/exact, containment, relative-position, align, distribute, with Dagre boxes only as starting suggestions.
    - Test 2: A weaker constraint that would move a locked node keeps the locked box unchanged and emits `constraints.locked-target-not-moved`.
    - Test 3: Non-finite exact positions emit `constraints.position.invalid` and do not write NaN or Infinity to output boxes.
    - Test 4: An unresolved overlap after deterministic adjustment emits `constraints.overlap.unresolved`.
  </behavior>
  <action>Create `src/constraints/solver.ts` exporting `applyLayoutConstraints(input: ConstraintSolverInput): ConstraintSolverResult`. Copy input boxes into a local `Map<string, Box>` without mutating input. Build locks from `NormalizedNode.position` with source `"fixed-position"` first, then from `exact-position` constraints with source `"exact-position"`. Apply constraints in this exact order: fixed positions / exact-position, containment, relative-position, align, distribute. For relative positions, use the referenced box plus optional offset; relations are `above`, `right-of`, `below`, and `left-of`. For align, support axes `x`, `y`, `center-x`, `center-y`, `top`, `right`, `bottom`, and `left`. For distribute, sort target boxes by x for horizontal and by y for vertical; if `spacing` is present use that spacing, otherwise distribute evenly between first and last boxes. Validate postconditions after each family and emit stable diagnostics instead of silently overwriting locks. Add a deterministic overlap pass that checks every pair with `intersectsAabb`; if one side is unlocked and one locked, push the unlocked box along diagram-neutral positive x by `overlapWidth + 40`, then diagnose remaining overlaps with `constraints.overlap.unresolved`. Do not import Dagre, Pretext, SVG, HTML, CSS, Excalidraw, Mermaid, or draw.io code.</action>
  <acceptance_criteria>
    - `rtk rg -n 'export function applyLayoutConstraints' src/constraints/solver.ts` finds the exported solver.
    - `rtk rg -n 'constraints\\.position\\.invalid|constraints\\.conflict\\.exact-position|constraints\\.containment\\.impossible|constraints\\.overlap\\.unresolved' src/constraints/solver.ts test/constraints.test.ts` finds implementation and test coverage for diagnostic codes.
    - `rtk rg -n 'exact-position.*containment.*relative-position.*align.*distribute|applyExact|applyContainment|applyRelative|applyAlign|applyDistribute' src/constraints/solver.ts` shows the locked precedence is encoded.
    - `rtk npm test -- constraints` passes.
  </acceptance_criteria>
  <verify>
    <automated>rtk npm test -- constraints</automated>
  </verify>
  <done>Constraint solving preserves hard locks, applies every Phase 3 constraint kind, and reports conflicts with stable diagnostics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| normalized diagram -> constraint solver | Untrusted positions, constraints, and references modify candidate layout boxes. |
| constraint diagnostics -> coordinated solver | Diagnostics decide whether a partial diagram is safe for downstream routing/export. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `src/constraints/solver.ts` | mitigate | Validate fixed and exact positions with finite-number checks; emit `constraints.position.invalid` and preserve previous finite boxes. |
| T-03-02 | Tampering | `src/constraints/solver.ts` | mitigate | Validate every target/source/reference/container/child ID before mutation; emit `constraints.reference.missing` for malformed references. |
| T-03-03 | Denial of Service | `src/constraints/solver.ts` | accept | Constraint pass is bounded to deterministic loops over constraints and box pairs; no unbounded solver or physics loop is introduced. |
</threat_model>

<verification>
Run `rtk npm test -- constraints`. At wave end, run `rtk npm run typecheck && rtk npm test -- layout constraints routing solver determinism && rtk npm run lint` once all same-wave test files exist.
</verification>

<success_criteria>
- Fixed positions and exact-position constraints are hard locks.
- Constraint precedence is implemented exactly as fixed/exact, containment, relative, align, distribute.
- Missing references and conflicts create diagnostics with stable code strings.
- Output boxes never contain NaN or Infinity after recoverable invalid input.
</success_criteria>

<output>
After completion, create `.planning/phases/03-layout-constraints-and-routing/03-02-SUMMARY.md`.
</output>
