---
phase: 03-layout-constraints-and-routing
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routing/index.ts
  - src/routing/types.ts
  - src/routing/routes.ts
  - test/routing.test.ts
autonomous: true
requirements: [RTE-01, RTE-02, RTE-03]
must_haves:
  truths:
    - "User can generate straight connector paths between shape ports."
    - "User can generate orthogonal connector paths by default using deterministic candidate elbows."
    - "Orthogonal routes avoid source/target interiors and simple rectangular obstacle boxes in fixture-style diagrams."
    - "Connector point arrays are simplified by removing consecutive duplicates and collinear middle points without reordering route order."
  artifacts:
    - path: "src/routing/routes.ts"
      provides: "Straight and orthogonal routing"
      exports: ["routeEdge", "simplifyRoute"]
    - path: "src/routing/types.ts"
      provides: "Renderer-neutral route input/output contracts"
      exports: ["RouteKind", "RouteEdgeInput", "RouteEdgeResult"]
    - path: "test/routing.test.ts"
      provides: "RTE-01/RTE-02/RTE-03 numeric tests"
      contains: "simplifyRoute"
  key_links:
    - from: "src/routing/routes.ts"
      to: "src/geometry/shapes.ts"
      via: "computeShapeGeometry and getEdgePort"
      pattern: "getEdgePort"
    - from: "src/routing/routes.ts"
      to: "src/geometry/boxes.ts"
      via: "AABB obstacle checks"
      pattern: "intersectsAabb"
---

<objective>
Create the renderer-neutral routing layer for straight and orthogonal connector paths.

Purpose: This satisfies RTE-01 through RTE-03 using Phase 2 anchors, deterministic candidate elbows, simple AABB obstacle checks, and point-order-preserving simplification.
Output: `src/routing/*` and `test/routing.test.ts`.
</objective>

<execution_context>
@/home/zhangyangrui/.codex/get-shit-done/workflows/execute-plan.md
@/home/zhangyangrui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-layout-constraints-and-routing/03-CONTEXT.md
@.planning/phases/03-layout-constraints-and-routing/03-RESEARCH.md
@.planning/phases/03-layout-constraints-and-routing/03-VALIDATION.md
@.planning/phases/03-layout-constraints-and-routing/03-PATTERNS.md
@.planning/phases/02-text-labels-and-shape-geometry/02-CONTEXT.md
@src/ir/elements.ts
@src/ir/geometry.ts
@src/geometry/boxes.ts
@src/geometry/shapes.ts
@src/serialization/canonical.ts

<interfaces>
Existing shape contracts:

```typescript
export function computeShapeGeometry(input: ShapeGeometryInput): ShapeGeometry;
export function getEdgePort(
  geometry: ShapeGeometry,
  toward: Point,
  preferredAnchor?: AnchorName,
): Point;
```

Create these routing contracts:

```typescript
export type RouteKind = "orthogonal" | "straight";

export interface RouteEdgeInput {
  kind?: RouteKind;
  direction: DiagramDirection;
  source: ShapeGeometry;
  target: ShapeGeometry;
  sourceAnchor?: AnchorName;
  targetAnchor?: AnchorName;
  obstacles?: readonly Box[];
}

export interface RouteEdgeResult {
  points: Point[];
  diagnostics: Diagnostic[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define route contracts and simplifier tests</name>
  <files>src/routing/index.ts, src/routing/types.ts, test/routing.test.ts</files>
  <read_first>
    - `src/routing/index.ts` if it exists; otherwise confirm the path is new with `rtk test ! -e src/routing/index.ts`
    - `src/routing/types.ts` if it exists; otherwise confirm the path is new with `rtk test ! -e src/routing/types.ts`
    - `test/routing.test.ts` if it exists; otherwise confirm the path is new with `rtk test ! -e test/routing.test.ts`
    - `src/ir/elements.ts`
    - `src/ir/geometry.ts`
    - `src/geometry/shapes.ts`
    - `src/serialization/canonical.ts`
    - `.planning/phases/03-layout-constraints-and-routing/03-CONTEXT.md` decisions D-17 through D-20
    - `.planning/phases/03-layout-constraints-and-routing/03-VALIDATION.md` row 03-03-01
    - `.planning/phases/03-layout-constraints-and-routing/03-PATTERNS.md` sections `src/routing/index.ts` and `src/routing/types.ts`
  </read_first>
  <behavior>
    - Test 1: `simplifyRoute()` removes consecutive duplicate points.
    - Test 2: `simplifyRoute()` removes a middle point when previous/current/next are collinear on x or y.
    - Test 3: `simplifyRoute()` preserves semantic route order and never sorts points.
  </behavior>
  <action>Create `src/routing/types.ts` with `RouteKind`, `RouteEdgeInput`, and `RouteEdgeResult` exactly as described in the interface block. Create `src/routing/index.ts` exporting `./routes.js` and `./types.js`. Create `test/routing.test.ts` with failing tests for `simplifyRoute()` duplicate removal, collinear middle-point removal, and route order preservation. Keep route options renderer-neutral; do not add route style to `NormalizedEdge` unless the solver integration plan needs it.</action>
  <acceptance_criteria>
    - `rtk rg -n 'export type RouteKind = \"orthogonal\" \\| \"straight\"' src/routing/types.ts` finds the route kind.
    - `rtk rg -n 'export interface RouteEdgeInput|export interface RouteEdgeResult' src/routing/types.ts` finds both contracts.
    - `rtk rg -n 'preserves.*order|semantic route order|collinear|duplicate' test/routing.test.ts` finds simplification coverage.
    - `rtk npm test -- routing` initially fails before `simplifyRoute` exists, then passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>rtk npm test -- routing</automated>
  </verify>
  <done>Routing type contracts exist and tests define point-order-preserving simplification behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement straight and orthogonal routing</name>
  <files>src/routing/routes.ts, test/routing.test.ts</files>
  <read_first>
    - `src/routing/routes.ts` if it exists; otherwise confirm the path is new with `rtk test ! -e src/routing/routes.ts`
    - `test/routing.test.ts`
    - `src/routing/types.ts`
    - `src/geometry/shapes.ts`
    - `src/geometry/boxes.ts`
    - `.planning/phases/03-layout-constraints-and-routing/03-CONTEXT.md` decisions D-17 through D-20
    - `.planning/phases/03-layout-constraints-and-routing/03-RESEARCH.md` sections "Pattern 4: Candidate Orthogonal Routing", "Pitfall 4", and "Pitfall 5"
    - `.planning/phases/03-layout-constraints-and-routing/03-PATTERNS.md` section `src/routing/routes.ts`
  </read_first>
  <behavior>
    - Test 1: `routeEdge({ kind: "straight" })` returns `[sourcePort, targetPort]` using `getEdgePort()`.
    - Test 2: `routeEdge({ kind: "orthogonal" })` returns a simplified orthogonal path where each segment is horizontal or vertical.
    - Test 3: Omitting `kind` defaults to orthogonal.
    - Test 4: Orthogonal route candidates reject paths crossing obstacle boxes and choose a later deterministic candidate when it avoids the obstacle.
    - Test 5: Obstacle-heavy input uses a finite deterministic candidate list and never starts grid A* or unbounded search.
  </behavior>
  <action>Create `src/routing/routes.ts` exporting `routeEdge(input: RouteEdgeInput): RouteEdgeResult` and `simplifyRoute(points: readonly Point[]): Point[]`. Use `getEdgePort(source, target.center, sourceAnchor)` and `getEdgePort(target, source.center, targetAnchor)` for endpoints. For straight routes, return the two ports through `simplifyRoute`. For orthogonal routes, generate candidates in this stable order: horizontal-first elbow `[source, {x: target.x, y: source.y}, target]`, vertical-first elbow `[source, {x: source.x, y: target.y}, target]`, direction dogleg through midpoint x for `TB`/`BT`, direction dogleg through midpoint y for `LR`/`RL`. Reject a candidate when any segment's bounding box intersects an obstacle box other than legal endpoint contact with source/target; use `intersectsAabb()` with zero-thickness segments expanded to at least one numeric line box for checking. If no candidate avoids obstacles, return the first candidate and an `info` or `warning` diagnostic code `routing.obstacle.unavoidable`. Do not implement full grid A* or any loop that grows with open search nodes.</action>
  <acceptance_criteria>
    - `rtk rg -n 'export function routeEdge|export function simplifyRoute' src/routing/routes.ts` finds both exported functions.
    - `rtk rg -n 'routing\\.obstacle\\.unavoidable' src/routing/routes.ts test/routing.test.ts` finds fallback diagnostic coverage.
    - `rtk rg -n 'getEdgePort|intersectsAabb' src/routing/routes.ts` confirms Phase 2 helpers are reused.
    - `rtk rg -n -i 'a\\*|astar|grid' src/routing src/geometry test/routing.test.ts` returns no implementation references to full grid pathfinding.
    - `rtk npm test -- routing` passes.
  </acceptance_criteria>
  <verify>
    <automated>rtk npm test -- routing</automated>
  </verify>
  <done>Straight and default orthogonal routes use shape ports, avoid simple obstacles through bounded candidates, and simplify point arrays without reordering them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coordinated node geometry -> routing | Node boxes, anchors, and obstacle boxes become connector path points. |
| obstacle list -> route candidates | User-provided or solver-derived obstacles influence candidate rejection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `src/routing/routes.ts` | mitigate | Reuse `computeShapeGeometry()` and `getEdgePort()` validation; do not emit non-finite route points. |
| T-03-02 | Tampering | `src/routing/routes.ts` | transfer | Node/edge reference validation belongs to solver before calling routing; routing validates geometry objects and anchors. |
| T-03-03 | Denial of Service | `src/routing/routes.ts` | mitigate | Use a fixed deterministic candidate list and reject full grid A* or unbounded search; tests grep for forbidden pathfinding terms. |
</threat_model>

<verification>
Run `rtk npm test -- routing`. At wave end, run `rtk npm run typecheck && rtk npm test -- layout constraints routing solver determinism && rtk npm run lint` once all same-wave test files exist.
</verification>

<success_criteria>
- `routeEdge()` supports `"straight"` and `"orthogonal"`.
- Orthogonal is the default route kind.
- Route endpoints come from Phase 2 ports/anchors.
- `simplifyRoute()` removes duplicates/collinear points while preserving route order.
- No full grid A* implementation appears in Phase 3 routing code.
</success_criteria>

<output>
After completion, create `.planning/phases/03-layout-constraints-and-routing/03-03-SUMMARY.md`.
</output>
