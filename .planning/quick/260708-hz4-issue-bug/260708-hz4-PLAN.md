# Quick Task 260708-hz4: Issue #71 Dense Routing Algorithm Follow-up

## Goal

Analyze the latest Issue #71 follow-up for `@crazyhappyone/auto-graph@0.2.14`, research mature algorithms for dense orthogonal obstacle routing with large nodes/labels, and continue PR #72 with a root-cause fix rather than a local bypass.

## Latest Issue Signal

- `0.2.14` improved Stage 5 validation from 472 to 277 criticals, but the downstream hard gate still fails.
- Remaining criticals: 255 `edge_route_text_intersection`, 22 `edge_route_obstacle_intersection`.
- Diagnostics still include 172 `routing.text-clearance.unresolved`, 169 `routing.obstacle.unavoidable`, 32 `routing.backtracking_excessive`, and 9 `routing.label-congestion.unresolved`.
- Worst remaining pages shifted to OV/resource-flow/activity/state views, so fixes must not be limited to CV/SV dependency cases.

## Plan

1. Research and summarize established approaches: object-avoiding orthogonal connector routing, orthogonal visibility/A* routing, monotonic route restrictions, space-driven vs center-driven costs, bus/rail routing, and integrated label-aware routing.
2. Compare those approaches to the current implementation in `src/routing/*` and `src/solver/solve.ts`.
3. Implement an algorithmic route-selection improvement: evaluate route candidates with a shared cost model covering hard crossings, endpoint interior re-entry, soft/text obstacle crossing length, path length, bend count, and backtracking distance.
4. Preserve hard safety: routes that cannot avoid dense large-node/label obstacles must carry structured diagnostics instead of being silently accepted as deliverable.
5. Add regressions for large/explicit endpoint cases and cost-driven fallback behavior.
6. Keep existing user/external changes intact, including the local `package.json` version bump to `0.2.14`.
7. Run focused tests, then `npm run verify`.
8. Update the quick summary/state artifacts, commit code and docs separately as appropriate, push the current PR branch, and re-check PR CI.

## Must Haves

- Continue on open PR #72 branch `codex/issue-71-edge-label-avoidance`.
- Do not remove user-created content or unrelated local changes.
- Include a research artifact for the chosen algorithmic direction.
- Add regression coverage for the bug fix.
- `npm run verify` passes locally.
- Push the completed fix to the PR branch and report the result.
